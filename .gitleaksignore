# .gitleaksignore — seeded by scaffold-generate (the project accretes real entries over time).
#
# TDD fixtures are false-positive-heavy by nature (fake keys in test data, recorded HTTP cassettes,
# example configs). When secrets-guard blocks a commit on a FIXTURE value:
#   1. Confirm it is genuinely fake/test-only (a real secret gets rotated, never ignored).
#   2. Take the `Fingerprint:` line from the gitleaks finding output.
#   3. Append it below, one fingerprint per line, with a trailing comment saying what it is.
#   4. Re-run the commit — the hook re-scans and passes.
# Never ignore by rule-id or path glob — fingerprints pin one specific finding, which is the point.
#
# Example (delete once real entries exist):
# 0123456789abcdef0123456789abcdef01234567:tests/fixtures/stripe-cassette.json:generic-api-key:12  # recorded test cassette, fake key

# P1.2 event-store redaction-scrub test (kernel-003) — fake `sk-` fixtures the redaction test MUST
# contain to prove the frozen scrub's key-format layer fires. Not real keys (sequential-alphabet).
# (Bare fingerprint per line — gitleaks matches the whole line, so NO inline trailing comments.)
# line 14 = fake sk- fixture in test_composes_frozen_scrub_layers
apps/api/test/unit/event-store/redaction.test.ts:generic-api-key:14
# line 89 = fake sk- fixture in test_does_not_mutate_input
apps/api/test/unit/event-store/redaction.test.ts:generic-api-key:89

# P1.3 append-only writer test (kernel-007) — fake DB-password fixture (DB_SECRET) the scrub-before-
# insert test MUST contain to prove the env-value scrub runs before persist. Not a real secret.
apps/api/test/integration/event-store/append.test.ts:generic-api-key:19
