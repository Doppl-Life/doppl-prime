---
# STOCK FIELD — the soil vines grow out of. Pairs with the flow docs.
# A RENDERED PROJECTION of an append-only findings ledger. NOT hand-grown.
# Holds promoted discoveries for one field, regardless of which chain/step found them.
#
# TWO GATES (both non-deterministic kernel calls; both must log WHY they ruled):
#   1. ADMISSION (high bar): action/search -> find (easily-found, low-novelty) -> discovery (key, grounded).
#      Only discoveries enter. find -> discovery clears novelty AND grounding = the flow->stock boundary.
#   2. ENRICHMENT (among discoveries): rehash -> drop · enrichment -> merge · new -> add region.
#      Protect breakout seeds; never let "tidy" sterilize the field.
id: b8e2c6f0-3a1d-4c9e-8b7a-5f2d1e0c9a4b   # UUIDv4 · durable key; links point here, NOT at the name
name: "Battery supply"       # human display name; can change without breaking links
keywords: [autonomy, accident-economy, insurance, ownership, residuals]   # retrieval handles
discoveries: 37              # promoted into the field (projection of the ledger)
finds_screened: 410          # considered but mostly not promoted — proof the bar is doing work
created: 2026-06-21
updated: 2026-06-22
---
# Battery supply

**Load-bearing facts**
<!-- promoted discoveries only · skim the bold; drop into the ledger by keyword for depth -->
- accident liability shifts from driver to manufacturer faster than premiums reprice  · grounded: 3 sources, 1 dated signal
- used-car residuals soften first in AV-pilot metros  · grounded: held-out metro data
- …
