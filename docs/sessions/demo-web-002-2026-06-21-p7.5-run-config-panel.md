# Session demo-web-002 — P7.5 operator run-config panel

- **Date:** 2026-06-21
- **Track / area:** `demo` / `apps/web` (implementer: demo-web-implementer)
- **Phase:** Phase 7 — Frontend dashboard (round 2)
- **Predecessor:** `demo-web-001-2026-06-21-p7-web-foundation.md`
- **Successor:** _(P7.6 run stop control — HALTED to round 3 by the round-2 close-out cycle)_

## Why this session existed

Round 2 of the demo web track: the operator run-config panel — the only screen that creates run
state. Closed out early by the team context-threshold ACTION cycle (P7.5 was the last round-2 slice;
P7.6 carries to round 3).

## What was built (1 slice, 1 commit)

| Slice | Commit | Summary |
|---|---|---|
| P7.5 | `a64f80c` | operator run-config panel — cap-max fail-closed + idempotent start |

### Files created
- `apps/web/src/components/run/runConfigForm.ts` — pure form→RunConfig mapping + `validateForm` (≥1-subtype, cap-max lowering-only, frozen-Zod shape) + `clampCap` + `CAP_CEILING` (mirrors API `defaultConfig.caps`).
- `apps/web/src/components/run/RunConfigPanel.tsx` — accessible form (validate-on-submit; inline `aria-invalid`/`aria-describedby` field errors), idempotent Start (per-submit `crypto.randomUUID` key; button disables while-starting/after-success), reflects the returned run id.
- `apps/web/test/unit/components/run/{runConfigForm.test.ts, RunConfigPanel.test.tsx}` — 8 tests.

### Files modified
- `apps/web/src/data/runClient.ts` — **additive** extension: `startRun(config, opts?: { idempotencyKey })` → sends an `Idempotency-Key` header (existing `startRun(config)` calls + the endpoint-allowlist test unchanged). Resolves the brief's internal conflict ("Modified: none" vs "startRun *with* an idempotency key"); orchestrator-approved.
- `apps/web/test/unit/data/runClient.test.ts` — added `test_start_run_sends_idempotency_key`; `fakeFetch` now captures request headers.

## Decisions made
- **Cap-max = fail-closed DEFENSE/UX, lowering-only** — `clampCap` clamps to `[1, CAP_CEILING]`; `validateForm` rejects any cap above the ceiling and never submits it. `CAP_CEILING` is a client constant mirroring the API `defaultConfig.caps`; the **API (P6.6) + kernel are the authoritative enforcers** (rule #1). Validate SHAPE via the frozen `RunConfig` Zod — never re-implement the contract.
- **Idempotent Start** — the panel generates a per-submit `crypto.randomUUID` key passed to `runClient.startRun` → `Idempotency-Key` header; the API dedups (not re-implemented). The button disables while-starting + after-success so a double-click can't fire a second `startRun`.
- **validate-on-submit (vs the prototype's hard-disable)** — Start is clickable; an invalid submit surfaces inline accessible errors that say WHY (the prototype's disabled button can't). Cap-over-ceiling + all-subtypes-off are still prevented at the input/toggle. (Recorded by the orchestrator as a prototype-fidelity improvement.)
- **wall-clock UX in minutes** → converted to the contract's `wallClockTimeoutMs` (×60 000) at build time.

## Decisions explicitly NOT made (deferred)
- **CAP_CEILING from a fetched maxima** — MVP uses a client constant mirroring the API defaults; swap for a config-maxima endpoint fetch when one exists.
- **RunConfigPanel persistent mount + real startRun wiring** → P7.14 shell + integration.

## TDD compliance
**Clean.** P7.5 was strict RED → Step-2.5 review → GREEN → Step-10 commit. The `runClient.startRun`
extension was RED-first (`test_start_run_sends_idempotency_key` failed on the missing header before the
extension). Post-GREEN edits were **test corrections only** (an `afterEach(cleanup)` so `screen` queries
don't see prior renders; `getByLabelText(/seed prompt/i)` to disambiguate from "RNG seed"; an
`exactOptionalPropertyTypes` `opts` type) — never implementation-before-test.

## Cross-doc invariant audit
**Clean — no model field changes.** P7.5 is a read-only consumer of frozen `RunConfig`/`RunCaps`;
`packages/contracts` was NOT edited. The `runClient` extension is an apps/web-internal seam change (a
request header), not a contract/Appendix-A change. "Cross-doc invariant change: NONE" flagged at Step 9.

## Reachability
- **runConfigForm / RunConfigPanel** — first consumer = the P7.14 shell (persistent mount); Start calls the real `runClient.startRun` at integration. *(tested-but-unwired → P7.14/integration)*
- **runClient.startRun extension** — reachable (the panel + the existing `startRun` consumers); additive, no behavior change for callers that omit the key.

## Open follow-ups
- **Future-TODO:** RunConfigPanel mount → P7.14 shell; real `startRun` wiring → integration; swap `CAP_CEILING` for a fetched config-maxima when an endpoint lands; P7.6 (run stop control — sibling run-control) → round 3.
- **Convention candidate (orchestrator-recorded in demo-orch-002; banks apps/web §4 if it recurs):** the browser cap-max check is fail-closed DEFENSE/UX (lowering-only); validate shape via the shared Zod, never re-implement the contract; the API+kernel are authoritative.

## Shared-worktree hygiene (two-impl)
Staged `apps/web/...` only (components/run/** + runClient.ts + the 3 test files). `pnpm-lock.yaml`'s `M`
is the observability impl's P6.x deps (P7.5 added none) — left untouched. No `apps/api/**` or
orchestrator-territory file staged.
