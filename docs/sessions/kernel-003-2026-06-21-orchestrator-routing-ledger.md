# Orchestrator routing ledger — kernel track (round in progress)

**Role:** kernel-runtime-orchestrator · **Date:** 2026-06-21 · **Branch:** track/kernel
**Purpose:** DURABLE record (survives an orchestrator cycle) of (a) cody-bound shared-root-doc edits that can't be written in this worktree, (b) track-local hot-writes already applied here, (c) live carry-forward. The lead reconciles git-derivable ticks at merge but NOT new doc-content items — those live here, not in session memory.

> **Why shared-root edits go to cody:** the kernel-worktree `IMPLEMENTATION_PLAN.md` / `ARCHITECTURE.md` are STALE fork copies; the authoritative copies live in the integration checkout (cody). The lead batches these at the next kernel→cody merge.

---

## A. Pending cody-bound shared-root-doc edits (hand to lead/cody at next merge)

> **✅ ROUND-1 (§A.1/§A.2/§A.2b/§A.2c/§A.2d/§A.3/§E/§F below) MERGED to cody 2026-06-21** — merge `36836a1` + reconciliation `2912819`, pushed origin/cody; lead-confirmed (ticks, §4/§6/§9 arch-notes, carry-forward triage, P1 phase-checkbox all applied). **Historical — do NOT re-apply.** New round-2 cody-bound items below.

### A0 — ROUND 2 (P3.1+) cody-bound items (pending next kernel→cody merge)
- **`IMPLEMENTATION_PLAN.md` tick: P3.1** `[ ]→[x]` — boot config loader; commit hash on done-wake (this round). Acceptance(P3) stays OPEN (P3 just started).
- **`ARCHITECTURE.md` §5/§15 note:** the boot sequence `migrate → loadConfig → seed → start`; `loadConfig` composes the canonical validators → a single **deep-frozen + readonly `AppConfig`**; the env→config path is a **CLOSED explicit allowlist** (no prefix sweep), credentials disjoint (env-only via `assertProviderCredentials`). (origin: P3.1)
- **Future-tidy (operational, low):** delegated validators `validateRunConfig` (frozen contracts) + `loadModelRegistry` (P2.2) still echo `issue.message` in errors — safe today (creds can't reach them; file sources operator-controlled, same trust tier). Re-point `loadModelRegistry` to the shared `summarizeZodIssues` if a less-trusted source is ever routed through it. (origin: P3.1 security-review observation)

### A.1 — `IMPLEMENTATION_PLAN.md` completed-work ticks (full kernel-track set pending cody)
Per lead merge plan (P2.2–P2.7 + P1.5–P1.8 + P1 phase-checkbox). cody no-ops any already-ticked.
- **P2.2** `[ ]→[x]` — role-keyed model registry; commit `8df860a` (prior round).
- **P2.5** `[ ]→[x]` — OpenRouter generation adapter; commit `5fd1c57` (prior round).
- **P2.6** `[ ]→[x]` — direct-OpenAI embedding adapter; commit `10a58d3` (prior round).
- **P2.7** `[ ]→[x]` — retrieval/web-search adapter; commit `67520ae` (this round).
- **P1.5** `[ ]→[x]` — **satisfied-by-P0** (no commit): EnergyEvent frozen P0.9 (`packages/contracts/src/domain/energy-event.ts`) + `energy.spent`→EnergyEvent narrowing P0.10 (`events/payload-map.ts:35`). Stale `events/energy-event.ts` path never existed. Lead-confirmed 2026-06-21.
- **P1.6** `[ ]→[x]` — **satisfied-by-P0** (no commit): NoveltyScore frozen P0.8 (`packages/contracts/src/scoring/novelty-score.ts`) + `novelty.scored`→NoveltyScore narrowing P0.10 (`events/payload-map.ts:39`) + embeddings-table-as-index shipped P1.4. Lead-confirmed 2026-06-21.
- **P1.7** `[ ]→[x]` — EvidenceRef resolver; commit `d3a61ed` (this round).
- **P1.8** `[ ]→[x]` — replay reader + canonical serialization; commit `dca9bc4` (this round). + fast-follow `86553c3` (kernel-014, the 2 phase-exit [medium]s).
- **Phase 1 checkbox** `[ ]→[x]` — **on the `/phase-exit P1` CLEAR verdict** (see §E). All P1 tasks done (P1.1–P1.4 shipped; P1.5/P1.6 satisfied-by-P0; P1.7/P1.8 shipped + hardened).

### A.2 — `ARCHITECTURE.md` §6 note (atomic with the round)
- Retrieval adapter (P2.7) **never-reject / always-curated-fallback** semantics: a terminal live-search failure falls back to the operator-curated corpus (tagged `fallbackSourced`), never throws `ProviderCallError`, never rejects (diverges from the P2.5/P2.6 throw→reject adapter leg; lesson §29). Empty curated match = valid empty result, not a failure.
- **Pluggable live-search seam** — no vendor pinned (user/lead directive); concrete provider deferred to the §6 retrieval spike. The retrieval credential loads **lazily in the live-client factory**, NOT in `assertProviderCredentials` (curated fallback needs no creds).

### A.2b — `ARCHITECTURE.md` §9 note (PENDING P1.7 land + Step-9 confirm)
- EvidenceRef resolver (P1.7) fail-closed taxonomy: `eventId` → resolve within the Postgres tier (`not_found` if absent); else a ref carrying ONLY `uri` **OR `langfuseObservationId`** → `external_only` (NEVER fetched — Langfuse is the §6/§13 non-authoritative side channel, replay never calls it); else (incl. `label`-only) → `no_pointer`. **Decision refines the kernel-012 brief** (which named only `uri`) — the langfuse-only leg was a Step-2.5 extension (folds the non-authoritative side-channel pointer into `external_only`, rule #7/§14).

### A.2c — `ARCHITECTURE.md` §9 note — replay reader (PENDING P1.8 land + Step-9 confirm)
- Replay reader (P1.8): **validate-not-sort** — asserts strictly-increasing + contiguous-from-0 `sequence` and throws `ReplayIntegrityError{ reason: 'gap' | 'out_of_order' | 'schema_too_new' }` (out_of_order checked before gap: `0,2,1`→out_of_order, `0,1,3`→gap); NEVER silently re-sorts/skips a corrupted authoritative log. Accepts `schemaVersion ≤ CURRENT_SCHEMA_VERSION` (older replays without upcasters), rejects `> current`.
- **Yield-shape decision:** `replayEvents` returns validated `RunEventRow[]` and does NOT re-parse to `RunEventEnvelope` — the P1.3 append path is the envelope-validation boundary (rule #2 append-only + P1.4 least-privilege make a stored row trustworthy by construction); replay's read-time invariants are ordering + schemaVersion only. Post-insert DB-tamper is P1.4's threat model, not replay's (so no 4th `malformed` reason). The fold consumes `RunEventRow` directly (consistent with `readByRun` + the P1.7 resolver).
- State-equivalence via `canonicalSerialize` (recursive key-sort, array order preserved). Rule #7 enforced structurally — the reader imports no provider/model/web seam (lesson 30).

### A.2d — `ARCHITECTURE.md` §4 note (STALE-DOC, from /phase-exit P1 arch-drift)
- §4 flow diagram says "ordered by run_id, sequence"; `readByRun` (`append.ts`) orders by `sequence` alone within a run-scoped `WHERE run_id = $1` — functionally equivalent (single-run query), diagram notation is loose. Tighten the diagram label or leave as-is (harmless). Architecture-doc note only; no code change.

### A.3 — `IMPLEMENTATION_PLAN.md` future-TODOs (add as phase tasks, anchored)
- **P3.1 boot:** wire `retrieval` into the role-dispatching providerCall — `createRetrievalProviderCall({ registry, client: undefined, corpus: DEFAULT_PRIOR_ART_CORPUS })`; part of the composite providerCall flagged in P2.6. (implements §6; origin: P2.7)
- **§6 retrieval spike (operational):** when a concrete live-search provider is wired — add its env var to the live-client factory's fail-fast check; decide whether it joins `assertProviderCredentials`' required set or stays optional (curated-fallback-always-available); set a real per-role timeout + cost/rate-limit envelope (RISK-004/005). (ops — §6)
- **P4/P5 consumers:** call `retrievalEvidenceRef(item, persistedEventId, kind)` at persist time + `searchCuratedCorpus(..., { kind })` with the consumer's kind (P4 prior_art / P5 signal). (implements §6/§7; origin: P2.7)
- **P6/PD + P1.8 consumers (EvidenceRef resolver):** dereference `evidenceRefs[]` via `resolveEvidenceRef` / `createEvidenceResolver` — create a FRESH resolver per projection/replay pass (the wrapper cache is read-once-per-run). P1.8 replay relies on the no-external-fetch property. (implements §9; origin: P1.7)
- **P6/PD consumers (replay reader):** P6 projection builders inject their real current-state/lineage folds into `replayRun(rows, fold, init)`; PD = the recorded-event replay-fallback (seed-to-summary) demo. **Fold states MUST be JSON-safe** (Dates OK — `toJSON`-normalized by `canonicalSerialize`; BigInt/circular throw loud). (implements §9; origin: P1.8)
- **P6 — event-store barrel write-gate ([low], from /phase-exit P1 code-quality):** `apps/api/src/event-store/index.ts` `export * from './schema'` re-exports all 12 table objects ungated; when P6 projection-builders land, add a write-gate so a builder can't `db.insert(<authoritative table>)` outside the append path (rule #2). (implements §9; origin: /phase-exit P1)

---

## B. Track-local hot-writes ALREADY applied in this worktree (commit at /orchestrate-end)
- **LESSONS §29** — "demo-safety fallback adapter" (fallback-not-reject; diverges from §28) → `apps/api/LESSONS.md` + index row in `apps/api/CLAUDE.md`. Pin: `apps/api/test/unit/model-gateway/adapters/retrieval.adapter.test.ts`.
- **LESSONS §30** — "replay-safety by construction" (rule #7 via purity/no-seam; informs P1.8) → `apps/api/LESSONS.md` + index row in `apps/api/CLAUDE.md`. Pin: `apps/api/test/unit/event-store/evidence-resolver.test.ts`.
- **LESSONS §31** — "validate-not-sort + toJSON-aware canonical equivalence" (the P1.8 [medium] Date-collapse) → `apps/api/LESSONS.md` + index row in `apps/api/CLAUDE.md`. Pin: `apps/api/test/unit/event-store/canonical-serialization.test.ts`.
- **LESSONS §26 + §31 tightenings (kernel-014)** — §26 forward-guard HARDENED (schema_invalid now path+code-only via `summarizeValidationIssues`; the real Zod-4 echo vector is `unrecognized_keys`; neutralized today by `z.record(z.string(),z.unknown())` payload) + §31 extended (toJSON ONCE per slot via `canonicalizeStructure` + DROP function/undefined so a surviving toJSON can't be re-invoked). No new lesson number. Pin: `apps/api/test/unit/event-store/{canonical-serialization,append}.test.ts`.
- **Briefs authored:** `docs/briefs/kernel-011-P2.7-retrieval-adapter.md` (spec-lint `@9f2037ee`) · `docs/briefs/kernel-012-P1.7-evidence-ref-resolver.md` (spec-lint `@da73b3c0`) · `docs/briefs/kernel-013-P1.8-replay-reader.md` (spec-lint `@25711b52`) · `docs/briefs/kernel-014-P1.8-followup-canonicalize-and-append-error-hardening.md` (spec-lint `@f9564577`).
- **This routing ledger:** `docs/sessions/kernel-003-2026-06-21-orchestrator-routing-ledger.md`.
- **(ROUND 2) LESSON §32** — "boot-config loader composition" (one `loadConfig` → deep-frozen `AppConfig`; creds-disjoint CLOSED env-allowlist; path+code errors) → `apps/api/LESSONS.md` + `apps/api/CLAUDE.md` index. Pin: `apps/api/test/unit/runtime/config/loadConfig.test.ts`.
- **(ROUND 2) LESSON §5 tightened** — single-source generalized from contract unions to ANY shared pure helper (extract-and-re-point at the 2nd in-track consumer; deepMerge + summarizeZodIssues); index row updated.
- **(ROUND 2) Brief authored:** `docs/briefs/kernel-015-P3.1-boot-config-loader.md` (spec-lint `@7ae738be`) · `docs/briefs/kernel-016-P0.15-amend-generation-status-degraded.md` (spec-lint amendment-false-positive, documented override).
- **(ROUND 2, kernel-016) cross-doc rows written hot (track-local):** `apps/api/CLAUDE.md` Run/Generation row (`GenerationStatus` closed 8→**9** +`degraded`) + RunEventEnvelope row (`CURRENT_SCHEMA_VERSION`=**3**, will → **4** at kernel-018) + LESSON §19 2nd-instance note (post-fork amendment playbook). Ride my next round seal.
- **(ROUND 2) LESSON §33** — "state-machine transition guards" (pure (from,to)→decision over a per-machine table = §3 spec; terminal-first; semantic preconditions in the kernel) → `apps/api/LESSONS.md` + index. Pin: `apps/api/test/unit/runtime/state/generationStateMachine.test.ts`.
- **(ROUND 2) Briefs authored:** kernel-017 (P3.2 state machines, spec-lint `@9b285cf0`) · kernel-018 (P0.5-amend CandidateStatus +repairing v4, amendment-false-positive override).
- **(ROUND 2) cody-bound `ARCHITECTURE.md` §3/§5 note:** guard-vs-loop responsibility split — guards DECIDE (pure (from,to)); the loop emits + the appender persists; the agenome fitness-score precondition is a P3.10 loop gate (§6), not the pure guard. (origin: P3.2)
- **(ROUND 2, kernel-018) cross-doc rows written hot (track-local):** `apps/api/CLAUDE.md` CandidateIdea row (`CandidateStatus` closed 8→**9** +`repairing`) + RunEventEnvelope row (`CURRENT_SCHEMA_VERSION`=**4**) + LESSON §19 3rd-instance + the **bump-vs-fold rule** (clean follow-up bump beats re-opening a committed version's fixtures; diff every §3 status vs its frozen enum at the freeze gate). Ride my next round seal. **Gap class CLOSED** — both enum amendments done; all 4 machines match §3.

---

## C. Live carry-forward (re-derive at consuming phase)
- **P3-gateway (NEXT SLICE — re-derive from LESSONS §27/§28/§32 + gateway code):** role-dispatching `providerCall` composing per-role adapters (openrouter/openai-embedding/retrieval) behind `createGateway`, consuming `AppConfig` (registry + creds) + `assertProviderCredentials` first; **per-role-timeout = adapter config** (not a `ModelRoute` field). **`single-source deepMerge` RESOLVED in P3.1** (`shared/deep-merge.ts`, registry re-pointed) — no longer open.
- **Held-out judge LOAD path (cross-track → verifier P4 / selection P5):** validate `FinalJudgeRubric` from immutable config (never an agent-writable path) + assert full 5-axis set + `immutableToAgents:true` before scoring (rule #6). [predecessor carry-forward]
- **✅ RESOLVED FINDING #2 (CandidateStatus missing `repairing`):** lead **PRE-AUTHORIZED** (applied the user's degraded ruling to this identical §3-FIX-edge class; user flagged for awareness, no fresh round-trip; same scoped-exception terms). Lead ran the FULL §3-vs-enum audit independently — confirms **gap class BOUNDED at 2** (Run 8/8✓, Agenome 7/7✓, Generation degraded done, Candidate repairing pending). **MECHANIC: clean v3→v4 bump** (lead ACCEPTED v4 — messages crossed; the lead reasoned a clean follow-up beats re-opening `a1da497`'s committed v3 fixtures, and it does ONE merge carrying v3+v4 anyway). **kernel-018 authored = v4** (P0.5-amend, CandidateStatus 8→9, `CURRENT_SCHEMA_VERSION` 3→4; mechanically identical to kernel-016). Dispatch after P3.2-partial commits → then kernel-019 (candidate machine, completes P3.2). At kernel-018 Step 9, re-flag lead "amendment ready (v4)" for the single v3+v4 merge.
- **✅ RESOLVED FINDING (GenerationStatus missing `degraded`):** lead+USER-ratified **option (a) amend**, **kernel owns it** (scoped exception to no-contract-edit, THIS amendment only). Dispatched **kernel-016 / task #16** (`amend_generation_status_add_degraded`): `GenerationStatus` 8→9 (+`degraded`) + `CURRENT_SCHEMA_VERSION` 2→3 + snapshot + fixtures, lesson §19 playbook. **UNBLOCKS P3.2** (lands before it). Lead handles cross-track propagation on the Step-9 "amendment ready" flag. (Original finding: frozen enum omitted `degraded` that §3 line 144 + P3.2 bullet 7 require.)
- **⚠️ CROSS-TRACK CARRY-FORWARD → P3.5 (verifier→kernel scrub fix; lead-verified `cody redaction.ts:88` 2026-06-21, user-ratified; FOLD INTO THE P3.5 BRIEF):** frozen P0.2 `scrubSecrets` whole-redacts any sensitive-key value to `'[REDACTED]'`, and `isSensitiveKey` SUBSTRING-matches `'token'` → `ProviderMeta.tokensIn/tokensOut` (z.int) corrupt to strings → fail safeParse on READ (silent — append narrows pre-scrub). Breaks rules #2/#4/#7. **Hits the kernel the moment P3.5 builds `energy.spent ← EnergyEvent.providerMeta`.** Fix is **verifier-owned + user-ratified** (logic-only, no schemaVersion change, redaction snapshot re-recorded), landing via track/verifier→cody. **P3.5 brief MUST:** (1) `git merge cody` to PULL the fixed scrub BEFORE building energy.spent providerMeta — **NO divergent kernel fix** (lesson §21 duplicate-scrub trap); (2) add a **scrub→append→read round-trip assertion** pinning tokensIn/tokensOut survive as numbers. Not needed until P3.5; kernel-020 unaffected (fix not in cody yet).
- **⚠️ CROSS-TRACK CONSTRAINT — P2.8 + P2.3 (demo→kernel observability boundary; lead-verified 2026-06-21, durable):** `packages/observability` is DEMO-track territory (Track map). **Kernel P2.8 (Langfuse correlation adapter) MUST import + call the demo-owned scrub `scrubObservabilityPayload` + `createEmitBoundary` from `packages/observability` (demo commit `0e2f793`, on track/demo) and NEVER reimplement a second scrub** — two divergent scrubs = the LESSON 21 secret-key-leak class; §14 mandates ONE scrub per boundary. The P2.8 brief MUST pin "import the demo scrub, no reimplement" as an **acceptance + reachability bullet**. **Sequencing:** P2.8 has an UPSTREAM dep — demo's P6.5 scrub must be merged to cody (or pulled) BEFORE P2.8 builds. **Both P2.8 + P2.3 stay HELD; FLAG THE LEAD when reached** so it coordinates the demo→cody merge ordering. (P3 current work unaffected — clean kernel territory.)

---

## E. /phase-exit P1 — GATE RESULT (2026-06-21) → CLEAR (pending session-doc row)

Multi-track note: executed against git + code reality (kernel-worktree tracker is the stale fork). Verdict + Log entry + P1 phase-checkbox → **cody** (this section is the durable hand-off).

| # | Row | Result | Evidence |
|---|---|---|---|
| 1 | All phase task checkboxes ticked | ✅ | P1.1 `1c301b1` · P1.2 `1f79273` · P1.3 `8bcce9c` · P1.4 `ec3a549` · P1.5/P1.6 satisfied-by-P0 · P1.7 `d3a61ed` · P1.8 `dca9bc4` |
| 2 | Acceptance criterion met | ✅ | P1 acceptance (append-only+sequence+redaction+§4 contracts+migrations+embeddings-authoritative+evidence-in-tier+replay-state-equivalence) all shipped; no runtime smoke (boot=P3, MVP-acceptable) |
| 3 | `/preflight` clean | ✅ | lint+typecheck Done (both projects); unit contracts 163/163 + apps/api 101/101 |
| 4 | Cross-doc invariants verified | ✅ | P1 added NO Appendix-A field change (all consumed frozen P0.1/P0.5/P0.8/P0.9) |
| 5 | Reachability audit clean | ✅ CLEAR | `docs/audits/P1-reachability.md` — 35 exports, 0 silent gaps; all explicit-deferrals named to P3/P6/PD |
| 6 | Arch-drift audit clean (§4/§9/§14) | ✅ CLEAR | 0 drift; 1 STALE-DOC note (see below). (auditor returned inline; no file written) |
| 7 | Spec coverage | ✅ | `spec-lint tests P1` exit 0 (parser skipped anchor-subset on stale-fork header format; per-slice spec(§)-tagged pins present — contracts 163 incl. all snapshots) |
| 8 | Whole-system security review (qualifying) | ✅ CLEAR | `docs/audits/P1-security.md` — rules #2/#4/#7 hold across integrated surface; 4 per-slice fixes HELD; no residual |
| 9 | Dependency audit | ✅ | `pnpm audit --prod` → No known vulnerabilities |
| 10 | Perf budgets | ✅ n/a | deliberate deferral (REQ-NF-003) |
| 11 | Session doc(s) exist | ⏳ pending | P1.1–1.4 in kernel-002; P1.7/P1.8 → impl `/session-end` kernel-004 IN FLIGHT |
| 12 | Commits pushed | ✅ verify-only | not-ahead surprise; slice commits push at `/orchestrate-end` |

**VERDICT: CLEAR** on all gate rows (row 11 completes when kernel-004 session doc lands). No row FAILED → not BLOCKED.

**Code-quality reviewer (phase-boundary, NOT a gate row — advisory) — CLEAR-with-notes** (`docs/audits/P1-code-quality.md`). Findings to disposition (escalated to lead):
- **[medium] `canonical-serialization.ts:24`** — `canonicalize` re-enters itself on the `toJSON()` result, so a toJSON returning a toJSON-bearing object would call toJSON twice (JSON.stringify calls once per slot). VERIFIED real by me. Bites only when a P6 fold-state value has a nested-toJSON object; Date (returns string) unaffected. Fix: handle the toJSON result's array/object/primitive inline without re-checking toJSON. → **fast-follow recommendation.**
- **[medium] `append.ts:64`** — `schema_invalid` error interpolates `parsed.error.message` which can include Zod's `.received` value (e.g. a secret-shaped `actor`). LESSON 26 forward-guard. Latent (AppendError not currently persisted/emitted). Fix: emit issue `.path`/`.code` only. → **fast-follow recommendation.**
- **[low] `index.ts:8`** — `export * from './schema'` re-exports all 12 table objects ungated (a future projection-builder could `db.insert` without a gate). → **P6 carry-forward** (add write-gate when builders land).

**Arch-drift STALE-DOC note (→ cody, ARCHITECTURE §4):** the §4 flow diagram says "ordered by run_id, sequence"; `readByRun` orders by `sequence` alone within a run-scoped `WHERE` — functionally equivalent, diagram notation loose. Architecture-doc note only.

## F. Carry-forward triage recommendation (for cody — `IMPLEMENTATION_PLAN.md` "Carry-forward")
The kernel-worktree fork's Carry-forward (5 predecessor items) — recommended triage at merge (I can't edit the stale fork):
1. **IDs opaque/unbounded strings** → **KEEP** (cross-track). Kernel portion CONSUMED (P1.3 append ids parameterized; P1.7 resolver exact-equality match) — note that; P6/P7/PD projection/demo portion still open.
2. **Payload size/depth ceiling — P1 portion** → **DELETE** (consumed). P1.3 append path calls `validateEventPayload` before insert + rejects via `AppendError` (caller emits the violation) — `8bcce9c`. Residual Fastify `bodyLimit` request-byte gate → SPREAD to P6 (routes), `last-consumer-slice: P6.x`.
3. **`validateRunConfig` canonical boot-config entry** → **KEEP** (cross-track → kernel P3.1; not yet consumed).
4. **§14 env-VALUE redaction layer** → **KEEP**. Kernel portion CONSUMED (P1.2 — env-value scrub at the write boundary, reachability-pinned); demo portion (P6.5 Langfuse) open → DELETE when demo consumes.
5. **Opaque gateway passthroughs scrubbed at persistence boundary** → **KEEP** (P3 gateway wiring finalizes the reachability pin; the scrub already covers all payloads via P1.2/P1.3).
6. **Held-out judge LOAD path validates rubric** → **KEEP** (cross-track → verifier P4/P5; not consumed).
Net: 1 DELETE, 1 SPREAD-residual, 4 KEEP (3 with "kernel portion consumed" notes). Carry-forward stays ≤7.

## G. 🚧 BLOCKER — cross-track schemaVersion collision (lead-found at kernel→cody merge, 2026-06-21; escalated to user)

Round-2 sealed `c2d5565` (pushed). The lead's kernel→cody merge is **BLOCKED**: cody advanced past the kernel fork with **P0.16** (judge-output seam — `judge.reviewed` event 36→37 + `JudgeResult`, **CURRENT_SCHEMA_VERSION→3**). The kernel forked BEFORE P0.16, so its amendments claimed v3 (degraded, kernel-016) + v4 (repairing, kernel-018) off the v2 base. **Disjoint changes, colliding version numbers** → 6 merge conflicts (version.ts, member-set snapshot, envelope test, fixtures, CLAUDE/LESSONS).

**Likely resolution (pending USER ruling, lead-proposed):** kernel pulls cody's P0.16 into track/kernel → **re-number** so **judge=v3, degraded+repairing=v4** (both kernel statuses fold into ONE version above judge) → re-record merged snapshots/fixtures → clean merge. Merged contract: RunEventType **37** (incl. judge.reviewed) + GenerationStatus **9** + CandidateStatus **9** + JudgeResult; final `CURRENT_SCHEMA_VERSION=4`. (Note: this re-numbering effectively does the degraded+repairing version-fold I'd originally drafted for kernel-018 before we chose separate v3→v4 bumps — the collision makes the fold the clean answer.) This is a **reconciliation slice** I + a fresh impl run once the user rules.

**Status (UPDATED — user GREENLIT rebase-onto-P0.16):** reconciliation brief **kernel-020 authored + ready** (`docs/briefs/kernel-020-contract-reconciliation-merge-cody-p0.16.md`). It's the FRESH impl's FIRST slice; **dispatch HELD until the lead confirms the fresh impl is oriented**. spec-lint = documented override (3 expected FAILs for a cross-track reconciliation: P0.15/P0.5 ticked amendment-false-positives + **P0.16 not-found** — it's cody's, absent from the kernel fork; the merge BRINGS it). Sequence: kernel-020 reconciliation (merge cody → UNION → judge=v3 / degraded+repairing=v4 / CURRENT_SCHEMA_VERSION=4 / RunEventType 37 + GenerationStatus 9 + CandidateStatus 9 + JudgeResult → re-record → full suite green) → flag lead for the clean kernel→cody merge → **then P3.4** (caps/kill-switch SOLO). Bank the §19 cross-track-version-serialization lesson at kernel-020 Step 9.

**Lesson-candidates (bank into LESSONS.md when I resolve the LESSONS.md merge conflict — both lead-endorsed):**
1. **Cross-track version collision (extends §19):** a shared monotonic contract counter (`CURRENT_SCHEMA_VERSION`) bumped INDEPENDENTLY in two parallel tracks off the same base COLLIDES at merge — even when the payload changes are disjoint. Serialize cross-track version bumps through the integration owner (or number-by-track + linearize at merge). The fix = UNION + linearize (judge=v3, kernel-statuses fold to v4).
2. **Cross-track merge stalls on orchestrator-territory doc conflicts (NEW, lead-flagged):** a `git merge` that conflicts in `apps/api/CLAUDE.md`/`LESSONS.md` is UNRESOLVABLE BY THE IMPL — the territory-guard hook blocks impl writes to those files BY DESIGN (working-as-intended). So a cross-track merge brief MUST bake in the **impl→orch handoff up front**: impl resolves the contract/code conflicts + greens, then HANDS the orchestrator-territory doc conflicts to the orch (who resolves them in-territory), then impl commits. Sequenced to avoid shared-worktree index collision. Without the up-front handoff the merge stalls mid-flight (as kernel-020 did). + the LESSONS member-number collision sub-rule (renumber the incoming/less-published lesson to the next free slot; here judge §32→§34).

**Doc-resolution status: DONE.** Impl resolved the 4 contract conflicts + greened (contracts 178/178 · apps/api 145/145 unit · 20/20 integration) + handed off. I resolved + staged the 2 doc conflicts by UNION: CLAUDE.md (RunEventEnvelope→v4 linearized; RunEventType→37 incl judge.reviewed; payload-map→7 incl judge.reviewed←JudgeResult; Run/Generation→GenerationStatus 9; lessons-index judge 32→34) + LESSONS.md (kept kernel §32 boot-config + §33 state-machines; renumbered cody's judge §32→§34). **ZERO `UU` remaining.** Impl committing the single merge commit (`merge(contracts): reconcile cody P0.16 + linearize schemaVersion→4`) → security-reviewer → Step 9. My kernel-003 ledger stays unstaged (out of the merge commit).
**Reconciliation outcome (merged surface):** RunEventType 37 + GenerationStatus 9 + CandidateStatus 9 + JudgeResult; `CURRENT_SCHEMA_VERSION=4` (v2 markers · v3 judge P0.16 · v4 degraded+repairing). Lessons §20–§31 identical both sides; §32 boot-config, §33 state-machines (kernel), §34 judge (renumbered from cody's §32).
**kernel-020 DONE:** merge `117a0ec` (2 parents: c2d5565 + cody d81a27e); task #20 completed; gate green (contracts 178/178 · apps/api 145/20); security CLEAN; 0 findings. §35 + §36 **BANKED** (LESSONS.md + CLAUDE index). Merged surface live on track/kernel: RunEventType 37 + GenerationStatus 9 + CandidateStatus 9 + JudgeResult; `CURRENT_SCHEMA_VERSION=4`; lessons §32 boot-config / §33 state-machines / §34 judge / §35 version-collision / §36 doc-handoff.

**HANDOFF for my SUCCESSOR (fresh orchestrator — I cycle at this 72% WARN boundary; impl CONTINUES, registry b3ce18e2):**
1. **2 unpushed commits on track/kernel** (origin = c2d5565): `117a0ec` (kernel-020 merge) + my close-out commit (§35/§36 + this ledger). **HOLD the origin push** — lead said "ping me first"; lead SEQUENCES the kernel→cody merge AFTER the imminent **verifier→cody** merge (scrub fix, §C) to avoid a race. Push track/kernel only on the lead's go; then lead does kernel→cody.
2. **Route ARCHITECTURE §4 "currently 1"→"4"** prose staleness to cody (shared-root; pre-existing both branches; impl-flagged at kernel-020 Step 9).
3. **Author + dispatch kernel-021 = P3.6 (seeded RNG + per-run-seed-in-run.configured + outcome persistence) FIRST** — USER RE-SEQUENCED P3.6 ahead of P3.4/P3.5 (lead-relayed). SOLO safety/determinism slice (rule #7). Deps satisfied: P0.3 `RunConfig.rngSeed` (frozen) + P1.3 append (done); does NOT need caps (P3.4) or energy (P3.5). The continuing impl (registry b3ce18e2) is ready. **NOTE my own dispatch was halted at HARD-STOP (82%) — kernel-021 is the SUCCESSOR's first dispatch.**
   - **Bonus (user-noted):** P3.6 carries NO ProviderMeta → the kernel→cody merge does NOT need the verifier scrub fix (§C) first; the scrub fix only matters at P3.5. Simplifies the merge.
4. **kernel→cody merge:** after P3.6 lands GREEN, **flag the lead — do NOT push.** The lead runs ONE bundled kernel→cody merge (kernel-020 reconciliation + P3.6) — announce-before-merge on cody, let any in-flight verifier→cody land first, `git merge cody`→green→merge kernel→cody→push.
5. P3 sequence after P3.6: **P3.4 caps/kill-switch (SOLO)** → **P3.5 energy (SOLO)** [FOLD the §C scrub-fix: pull cody's fixed scrub before energy.spent + round-trip assertion; NO divergent fix — lesson §21/§36] → gateway-composition (near P3.10) → P3.10/P3.12/P3.13 loop+worker+crash-forward.

## D. Round status
- **ROUND 1 — SEALED + MERGED.** Shipped P2.7 (`67520ae`) · P1.7 (`d3a61ed`) · P1.8 (`dca9bc4`) · kernel-014 (`86553c3`); round-seal `bd4068a` (pushed); impl doc kernel-004 (`561c9b5`). Phase 1 COMPLETE; `/phase-exit P1` CLEAR (§E). Merged to cody `36836a1`+`2912819`.
- **ROUND 2 — IN PROGRESS (Phase 3 open).** P3.1 boot config loader committed `db4b045` (task #15 done). Banked LESSON §32 (boot-config composition) + tightened §5 (single-source generalizes to shared utils) + resolved the deepMerge + zod-error-formatter single-source carry-forwards.
- **DONE: kernel-016 P0.15-amend** (`GenerationStatus` +`degraded`, schemaVersion 2→3) committed `a1da497`; "amendment ready" flagged to lead for cross-track merge.
- **IN FLIGHT: P3.2 (task #17) — PARTIAL.** 3 of 4 state machines (Run/Generation/Agenome) GREEN-ready → ship now as P3.2-partial (brief kernel-017, spec-lint `@9b285cf0`). **CANDIDATE machine BLOCKED** by FINDING #2 (CandidateStatus missing `repairing`, see §C) → follows the P0.5-amend.
- **RATIFIED (lead) → sequence:** kernel-018 (P0.5-amend, CandidateStatus 8→9 +`repairing`, **clean v3→v4 bump** [lead ACCEPTED v4 — NOT a fold; see crossing note]; authored, spec-lint amendment-false-positive override) → kernel-019 (candidate state machine, completes P3.2) → P3.4 caps (SOLO) → P3.5 energy (SOLO) → P3.6 RNG → gateway-composition (near P3.10, lesson 20) → P3.10/P3.12/P3.13 loop+worker+crash-forward.
- **MECHANIC CROSSING (resolved):** my kernel-018 draft was fold-into-v3 (per the lead's earlier strong preference); the lead's later msg ratified **v4** (believing I'd called v4) with a sound reason (don't re-open `a1da497`'s committed v3 fixtures; lead does ONE merge carrying v3+v4 anyway). Aligned to **v4** — rewrote kernel-018. Will note the crossing in the Step-9 "amendment ready" flag (honesty; the outcome is v4 either way).
- **P3.2 COMPLETE** (4 of 4 machines): Run/Generation/Agenome `b9ad31e` (kernel-017) + Candidate (kernel-019, committing). Tracker P3.2 `[ ]→[x]` → cody at next seal.
- **CODY-bound v3+v4 (ONE merge, from kernel-016 + kernel-018):** cross-doc invariant change — `apps/api/CLAUDE.md` Run/Generation row (GenerationStatus 8→**9** +`degraded`) [DONE, track-local] + CandidateIdea row (CandidateStatus 8→**9** +`repairing`) [write hot at kernel-018 Step 9] + RunEventEnvelope `CURRENT_SCHEMA_VERSION`→**4** [track-local] + `ARCHITECTURE.md` Appendix-A GenerationStatus + CandidateStatus + schemaVersion **2→4** [→ cody] + P0.15-amend + P0.5-amend Log entries [→ cody]. Lead: ONE kernel→cody merge (v3+v4) + notify verifier/selection/demo to re-record Generation(9)+Candidate(9) snapshots (additive, non-urgent).
- **Future-tidy (DX, low):** `scripts/spec-lint.sh` has no amendment mode — a freeze-amendment of a ticked task structurally FAILs the "unticked checkbox" check (hit by P0.1-amend + kernel-016). Could accept an `-amend` task-id suffix or a `amends frozen` brief declaration. Documented-override used meanwhile.
- **⚠️ WARN crossing (post-kernel-018 `afaab95`): impl=72%, cycle approaching.** Pinged lead (verbatim line); **kernel-019 dispatch HELD** for the lead's cycle decision: (a) run kernel-019 → complete P3.2 → cycle at P3.2-complete, or (b) cycle impl now (P3.2-partial) + I seal. **If the IMPLEMENTER cycles:** the fresh impl's FIRST slice = **kernel-019** (candidate state machine, brief NOT yet authored — author it: mirror the 3 shipped machines in `runtime/state/`, add `candidateStateMachine.ts` with `CANDIDATE_TRANSITIONS` per §3 [created→{under_review,repairing,invalid}, under_review→{checked,rejected}, checked→{scored}, scored→{selected,culled}, repairing→{under_review,invalid}; terminal {selected,rejected,culled,invalid}] via the shared `makeTransitionGuard`; `repairing` now live in CandidateStatus). Completes P3.2.
- **Suite (post-kernel-018):** contracts 166/166 + apps/api 140 unit / 20 integration.
- **A fresh round-2 ledger will be cut at the round-2 `/orchestrate-end`** (this doc's §A0 + §B/§C carry the round-2 cody-bound + live items meanwhile).

## H. ROUND 3 (successor orchestrator — P3.6) — live status (2026-06-21)
_(Successor `kernel-runtime-orchestrator` picked up after the predecessor cycled at HARD-STOP post-kernel-020. Impl `kernel-runtime-implementer` continues.)_
- **kernel-020 reconciliation MERGED to cody by the LEAD** (`bff4325` + doc reconcile `534c38f`, pushed origin/cody). sv4 cody-wide; verifier P0.2 scrub fix now IN cody; integration preflight CLEAR (kernel + verifier P4 coexist). The §G "HOLD push / sequence after verifier" concern is RESOLVED. **Push hold LIFTED:** push track/kernel→origin at the next round seal as normal; the LEAD owns track/kernel→cody.
- **P3.6 (kernel-021) — seeded RNG + live/replay outcome bridge. DONE + MERGED.** Brief authored (`docs/briefs/kernel-021-...`, spec-lint PASS `@a2fea852`); Step-2.5 approved (+1 ADD: pick index-semantics + replay bounds); Step-9 SHIP'd. GREEN: unit 156/156 · integration 20/20; security-reviewer (invariant) CLEAN. mulberry32 + `seed >>> 0`; `persistOutcomes.ts` PRNG-free (rule #7 structural). NO contract change. Committed `d5a3c32`; **MERGED to cody by the LEAD — `71163a1` (RNG code) + `376b4b0` (arch reconcile), pushed origin/cody; full preflight CLEAR 264 unit + 40 integration (kernel + verifier P4 + P3.6 coexist).**
- **Step-9 routed:** Lesson **§37** (seeded-RNG replay-determinism by construction) BANKED (LESSONS.md + CLAUDE index, pin `persistOutcomes.test.ts`). Scope-cut (raw `SeededRng.pick` dropped) ACCEPTED — safety-positive (rule #7 footgun, no consumer), no functional loss, no escalation. Emission future-TODO → already tracked by P3.9/P3.10/P3.12 (no new task). Cross-doc invariant: NONE.
- **CODY-BOUND ARCHITECTURE edits — ✅ ALL DONE in `376b4b0` (lead-applied):** (1) §4 "currently 1"→"4" prose + bump history; (2) Appendix-A GenerationStatus 9 (+degraded) + CandidateStatus 9 (+repairing); (3) §4 mulberry32 + `seed >>> 0` RNG note (my drop-in, verbatim). Nothing cody-bound remaining from P3.6.
- **P3.6 merge: ✅ DONE** (flagged lead → lead merged `71163a1`+`376b4b0`). **Next lead-flag NOT needed until the next round-seal/merge point — OR P3.5** (where the now-in-cody verifier scrub fix matters for energy.spent). **(Note: the shared task list went empty/unreliable this round — rely on this ledger + git log + the impl's done-wake, not the task list.)**
- **Still uncommitted track-local (ride next `/orchestrate-end` round seal — additive, not urgent):** LESSONS §37–§40 + kernel-021…025 briefs + this ledger §H + the CLAUDE.md lessons-index rows (§37–§40).
- **ROUND-3 COMMITTED SLICES:** P3.6 RNG `d5a3c32` · P3.4 caps+kill `dac730d` · P3.5 energy `bf99d59` · P3.9 clamp `fb7007c` · P3.9 seed set `7387227` · chore(format) `6cdde0c`. **P3.9 COMPLETE.** Suite 190/190 unit · 20/20 integration. Lessons §37–§40 banked.
- **🚧 NATURAL PAUSE (flagged lead 2026-06-21) — no unblocked standalone kernel work:** the deterministic substrate (P3.1/P3.2/P3.4/P3.5/P3.6/P3.9) is DONE. **P3.7 satisfied-by-gateway** (`withRetry` in `model-gateway/adapters/retry.ts` — bounded retry + per-role timeout + one fallback + terminal ProviderCallError, P2.x §28/§29). **P3.8 satisfied-by-gateway** (`gateway.ts` does the ≤1 structured-output repair internally — validate/repair/reject → validationResult accepted|repaired|rejected, P2.4). Both their runtime residuals (provider_call_failed EMISSION + candidate/agenome state-mapping of the gateway result + wall-clock-bounded retry) **fold into P3.10** — a separate runtime/retry/ or runtime/repair/ would DUPLICATE the gateway mechanism (L28/L21). Tick P3.7/P3.8 satisfied-by-gateway at the merge (like P3.3 satisfied-by-P1.3). **The only remaining kernel work = the sv4→5 amendment (BLOCKED on lead's candidate.rejected settle) + P3.10 loop (BLOCKED on the amendment + carries ALL the carry-forwards: drain-then-terminalize · latching-halt · scrub-fix git-merge-cody + round-trip · cost-map→AppConfig).** → Flagged lead: settle the amendment count + sequence the round seal + sv4→5 announce + kernel→cody merge. Context OK but climbing (impl 64%). Impl idle pending direction (correct per "Idle only when: a blocking dependency needs user direction").
- **AMENDMENT SETTLED + DISPATCHED (2026-06-21):** USER RATIFIED the FULL **4-event** amendment — `run.cancelled` + `generation.skipped` + `agenome.failed` + `candidate.rejected` (RunEventType 37→**41**, `CURRENT_SCHEMA_VERSION` 4→**5**). Brief **kernel-026** (`docs/briefs/kernel-026-P0.1-amend-terminal-events-sv5.md`), task **#26**, dispatched. spec-lint = documented amendment override (only the expected P0.1-ticked false-positive). §19 playbook. **Kernel wires only run.cancelled + generation.skipped (P3.4 killSwitch null→event flip);** `agenome.failed` emission = loop P3.10, `candidate.rejected` emission = VERIFIER (cross-track, registry-add only here). Cross-doc: orch writes CLAUDE.md schemaVersion→5/RunEventType→41 rows; Appendix-A → cody via lead. **Lead announce-before-merge in flight** (sv4→5 + 4 events to verifier/selection/demo + secure verifier's candidate.rejected blessing). **SEQUENCING (lead, final — bundled):** lead briefly floated DECOUPLE (seal substrate now, amendment next round) then REVERSED — **BUNDLE the amendment into THIS round** (the announce parallelizes, doesn't block the merge); ONE sv5 cody merge carries P3.4/P3.5/P3.6/P3.9 + amendment + lessons §37–§40 when kernel-026 green. (Orch's HOLD+revert on kernel-026 was retracted on the reversal — impl RESUMED.) On green → flag lead + send `/context-check` line → lead does the bundled sv5 merge + propagation + **decides the cycle (P3.10 next = big slice → likely cycle impl + orch at the seal).** **P3.10 HELD until the amendment merges + lead confirms the verifier candidate.rejected blessing.** Context climbing impl 64%. _(NOTE for successor: "kernel-024" in the lead's earlier msg is STALE — 024=clamp, 025=seed set, 026=this amendment.)_
- **✅ ROUND-3 SUBSTRATE SEALED + PUSHED (2026-06-21):** round commit **`ee8b3d6`** (orch docs: lessons §37–§40 + CLAUDE index + briefs kernel-021…026 + ledger §H) + impl session doc **`47d8035`** (kernel-006); pushed `c2d5565..ee8b3d6` (verified, tree clean). Context at seal = **ACTION (impl 75%) → cycle**. Lead acked: substrate kernel→cody merge clear (no contract change) + cody-bound ticks (P3.4/P3.5/P3.6/P3.9 + satisfied-by P3.3←P1.3/P1.4 · P3.7←gateway withRetry · P3.8←gateway.ts ≤1 repair). Awaiting lead's shutdown_request (round in + tree clean → approve).
- **🔧 candidate.rejected EMISSION — CORRECTED (lead + verifier-blessing RESOLVED, 2026-06-21):** SUPERSEDES the "emission = verifier" premise in the entries above + the kernel-026 brief (now corrected, committed). The verifier council/judge are evidence-only (rule #6) + the **verifier track is RETIRED** — it wires NOTHING. The `under_review→rejected` transition is the **KERNEL's own** (`runtime/state/candidateStateMachine.ts`, P3.2); `candidate.rejected` emission is the **RUNTIME's (P3), driven on SELECTION's (P5) verdict** (aggregates verifier evidence + judge acceptance). **MVP: defined-but-not-yet-emitted** (registry-add now). **The amendment (kernel-026) is now FULLY KERNEL-INTERNAL — NO cross-track blessing gate**; proceeds whenever next round opens. The sv4→5 announce still happens (additive snapshot re-record, non-urgent), but is not a gate.
- **DEFERRED CARRY-FORWARD → P3↔P5 seam (NOT a P3.10 must-wire):** `candidate.rejected` emission — the RUNTIME emits it on SELECTION's reject verdict (the P3 runtime ↔ P5 selection seam). Design it into that seam later; MVP ships defined-but-not-yet-emitted. _(origin: 2026-06-21 kernel-026 audit + lead verifier-blessing resolution)_
- **NEXT ROUND (successor's first slice):** re-dispatch **kernel-026** (sv4→5 amendment) — brief complete + corrected (validated file surface; kernel-internal, no blessing gate; documented spec-lint amendment override). Then **P3.10** loop (carries: drain-then-terminalize excluded states · latching-halt kill · L21 scrub-fix `git merge cody` + round-trip on energy.spent ProviderMeta · cost-map→AppConfig · the candidate.rejected P3↔P5 emission carry-forward above).
- **FUTURE TODO (belongs-to-a-phase, route to cody plan at /orchestrate-end):** wire `DEFAULT_COST_MAP` (`costMap.ts`, P3.5) into `AppConfig` so the cost map is boot-configurable (`tokensPerUnit/perToolCall/perSpawn` tunable post-spike, §4). Origin: 2026-06-21 P3.5 Step-2.5 (AppConfig has no cost-map section yet). Lands at P3.1-extend or P3.10 loop-wiring. _(implements §4; origin: kernel-023)_
- **P3.4 (kernel-022) caps/kill-switch — IN FLIGHT (Step-2.5, TWEAK'd).** Impl surfaced a load-bearing §5-vs-§3 reconciliation; resolved + escalated as a **FINDING** to the lead (for user confirmation, NON-blocking — impl proceeding). **Resolution (what's being built):** §5 "every non-terminal→failed/stopped" is a simplification; the killSwitch maps each non-terminal to its §3-legal terminal — RUN: `configured→cancelled`, `running→stopping`(operator)/`→failed`(cap+wall), `completing`/`stopping`→EXCLUDE (drain to their terminal); GEN: `pending→skipped`, `{running,verifying,scoring,reproducing}→failed`, `degraded`→EXCLUDE (one-shot transient, drains `→verifying→failed`). EXCLUDE = no legal direct kill-edge → drains via its in-flight step (consistent with §5 "drains in-flight"); NOT a table amendment. P3.4 = pure decide (capEnforcer/killSwitch/capLedger); emission deferred to P3.10/P3.12.
- **CARRY-FORWARD → P3.10 (generation loop) [lead-endorsed]:** the loop must **drain-then-terminalize** the kill-EXCLUDED states — completing→completed, stopping→stopped, degraded→verifying→failed — so "every non-terminal reaches terminal under kill" holds end-to-end. **LOAD-BEARING GUARD (lead):** the kill switch MUST stay **ARMED through the drain** — it is a **latching halt, not a one-shot edge**. So a drained transient (degraded→verifying) then hits verifying→failed under the *still-active* kill, and a drained state can NEVER re-arm into new productive work or stall mid-drain. Pin this in the P3.10 loop brief. _(origin: 2026-06-21 P3.4 Step-2.5 §5-vs-§3 finding; lead-endorsed + guard added)_
- **CODY-BOUND (later merge, low-pri):** ARCHITECTURE **§5** clarification — pin the kill-switch per-state trigger→terminal mapping (not a literal universal →failed): operator-stop → `configured→cancelled` / `running→stopping`; cap-breach/wall-clock → `running→failed`; generation `pending→skipped`, `{running,verifying,scoring,reproducing}→failed`; `completing→completed` / `stopping→stopped` / `degraded→verifying` drain via their in-flight step (kill is a latching halt). Pairs with the P3.10 carry-forward. (Lead-endorsed; lead applies at next kernel→cody merge.)
- **FINDING #2 — CONTRACT GAP → USER RATIFIED option (A) amend (kernel-owned scoped exception, same terms as degraded/repairing). FULL terminal-event audit DONE (lead-requested, no whack-a-mole):**
  - **GAP SET = 4 reachable terminals with NO recording event:** (1) **run.cancelled** (kernel, configured→cancelled); (2) **generation.skipped** (kernel, pending→skipped); (3) **agenome.failed** (kernel, active→failed — NEW from sweep); (4) **candidate.rejected** (VERIFIER P4, under_review→rejected — NEW; `critic.reviewed` can't carry a verdict by rule #6 → not projectable; **CROSS-TRACK confirm needed**).
  - **rule-#2-OK (NO new event):** candidate `selected` = PROJECTED (scored via fitness.scored ∧ ¬lineage.culled, §8 selection-explainable); candidate+agenome `culled` = lineage.culled (CullingEvent.targetIds generic); `invalid` = candidate_invalidated; `reproduced` = agenome.reproduced; run completed/stopped/failed + gen completed/failed event-backed.
  - **Correction:** AgenomeStatus `spent`/`eligible_parent` are INTERMEDIATE, not terminal (lead's list over-counted) — no terminal-event requirement.
  - **Amendment surface (sv4→5, §19):** add the events → RunEventType 37→**41** (or 40 if candidate.rejected projects) + update killSwitch terminalEvent:null→{run.cancelled, generation.skipped} + re-record member-set snapshot/fixtures. **Amendment = SEPARATE slice kernel-024 AFTER P3.5.** 3 kernel-settled; **candidate.rejected (3-vs-4) pending verifier confirm at the lead's sv4→5 announce.** Lead coordinates announce-before-merge + sv4→5 cody merge. _(origin: 2026-06-21 P3.4 Step-2.5 registry grep → full audit; user-ratified A)_
- **P3 sequence next:** P3.4 caps/kill-switch (SOLO) → P3.5 energy (SOLO) [`git merge cody` to pull the now-in-cody verifier scrub fix + round-trip assertion; NO divergent scrub — §21/§36; P3.6 needed none] → gateway-composition (near P3.10) → P3.10/P3.12/P3.13.

## I. ROUND 4 (fresh orchestrator `08c2575b` — resume-from-pause; impl `f24426c7`, lead `team-lead`) — live status (2026-06-22)
_(NOTE: the lead-cited handoff `kernel-001-2026-06-22-substrate-merged-pause.md` does NOT exist in this worktree — it lives on cody/integration, per memory "Cite handoff by integration path". Reconstructed the arc from §G/§H + git log; not blocking.)_

- **kernel-026 (sv4→5 terminal-event amendment) — DONE + MERGED to cody.** Committed `805ef9d` (RunEventType 37→**41** +run.cancelled/generation.skipped/agenome.failed/candidate.rejected; `CURRENT_SCHEMA_VERSION` 4→**5**; killSwitch null→named for the 2). candidate.rejected emission CORRECTED = RUNTIME-on-SELECTION-verdict (P3↔P5 seam, MVP-deferred; verifier evidence-only/rule#6 + retired → fully kernel-internal, no blessing gate). **Cross-doc hot:** CLAUDE.md RunEventEnvelope sv5 + RunEventType 41 + payload-map note (committed `fc17622`). **MERGE — SUPERSEDED Option-B:** lead reversed to **Option-A** on the user's go (demo round-4 observability→cody was blocked on sv5-in-cody, which also unblocks our P2.3/P2.8) → **scoped merge of 805ef9d ONLY → cody `06299c9`** (sv5 contracts + killSwitch; cody preflight GREEN; NOT pushed — demo lead gates the cody push). cody lesson numbering stays §37–50; verifier §46 scrub fix preserved. **DO NOT re-merge kernel-026→cody.**
- **P3.10 DECOMPOSED into sub-slices** (safety-invariant slices unbundled from feature work): **10a** cost-map→AppConfig ✓ · **10b** loop skeleton ✓ · **10c** edges (ready) · **10d** energy.spent+scrub+provider_call_failed [SAFETY rule#4/#8; git-merge-cody] · **10e** kill/cap/wall-clock abort + drain-then-terminalize + latching halt [SAFETY rule#1].
  - **10a (kernel-027) — DONE `e88bf6b`.** cost-map→AppConfig: Zod `CostMapConfigSchema` (z.strictObject) + `CostMapConfig`=z.infer (single-source L5); `AppConfig.costMap`; loadConfig composes defaults<file; deep-frozen; no-value-echo fail-fast (L26). Runtime-local config (NO Appendix-A row). suite apps/api 195u.
  - **10b (kernel-028) — DONE `6279526`.** `runGenerationLoop` (runtime/loop/generationLoop.ts): bounded happy-path lifecycle via P3.2 guards; appends ONLY kernel-owned events (generation.*/markers/tool_call relay/agenome.spawned/candidate.created) via the append path; **option-b inject-and-fake seams** (lead-confirmed: selection P5 NOT in cody — track/selection complete-but-paused/unmerged; verifier retired) — loop consumes seam events as DATA, never authors them. Bounded by maxGenerations+maxPopulation (enforceCap). LIVE outcome source for reproduction RNG. security-reviewer (invariant) PASS 0 findings. suite apps/api 204u. **D2 (new design):** tool_call relay rides a runtime-local `GenerationGateway` composing the frozen ModelGateway (frozen response has no tool-call field) — orchestrator-blessed, no contract edit.
  - **10c (kernel-029) — AUTHORED + spec-lint PASS `@84be8bc8`, ready to dispatch.** Edges: partial-failure (running→degraded→verifying, minPopulationSurvival), all-fail→failed, zero-survivors (survivors:0), degenerate reproduction (mutation_only) + **agenome.failed emission** (sv5 event's FIRST emitter). FEATURE.
- **LESSONS banked this round (track-local, committed `fc17622` §41 + pending-commit §42):** **§41** (audit ALL terminals vs the registry — kernel-026) · **§42** (loop = pure orchestration, never authors seam events — kernel-028). **⚠️ 10d RECONCILE:** at 10d's `git merge cody`, cody carries §37–50 (verifier §37–46 + kernel §47–50; track/kernel's §37–40 are DUPES of cody's §47–50 → DROP) → **renumber track-local §41→§51 + §42→§52** in BOTH LESSONS.md + CLAUDE.md index + fix refs. Impl→orch doc-handoff per L36 (impl resolves code/contract conflicts + greens + holds → orch resolves LESSONS.md/CLAUDE.md → impl commits the single merge).
- **10d git-merge-cody — ENLARGED BASE (lead heads-up):** by 10d, cody advances past `06299c9` — demo round-4 rebases onto cody-sv5 + merges `packages/observability` + their sv4/sv5 reducer reconcile. So 10d's pull = sv5 (no-op for our contracts) + verifier §46 scrub fix (L21, the reason for the merge) + observability + demo reconcile in one go. Additive; LESSONS/index is the only territory-doc hand-resolve. Bake into the 10d brief.
- **DEFERRED CARRY-FORWARDS (named, not dissolved):**
  - **Successor-population threading** (D3) — the loop consuming the reproduce seam's offspring (agenome.reproduced/fused/mutated) into the NEXT generation's population. Successor ALLOCATION is selection P5's job (faked); the loop THREADING is kernel's. DEFERRED to a dedicated later slice / real-selection wiring; **gen-0-only persists across N generations meanwhile** (valid orchestration, vacuous evolution). _(origin: kernel-028 D3)_
  - **Real `GenerationGateway` impl** surfacing provider tool calls — gateway/adapter-layer wiring (the runtime-local port + relay + fake shipped in 10b). _(origin: kernel-028 D2)_
  - **readByRun-per-generation perf** — O(events) re-read growth across generations (bounded by caps + payload-DoS ceiling for MVP); incremental read at P3.12 worker / a perf pass. _(origin: kernel-028 security-reviewer perf note, NOT a finding)_
- **CODY-BOUND ARCHITECTURE notes (→ lead at next kernel→cody):** §5 — the loop emits only kernel-owned events + consumes injected-seam events (option-b orchestration boundary); §6 — the loop↔gateway tool_call relay seam (runtime-local GenerationGateway composes the frozen ModelGateway). _(origin: kernel-028 Step-9)_
- **PUSH/MERGE state:** track/kernel tip carries 10a/10b + §41/§42 (NOT in cody — only the scoped kernel-026/805ef9d is). track/kernel→origin push at the round seal (/orchestrate-end); the LEAD owns track/kernel→cody (after demo round-4 + the 10d pull/reconcile). Orch docs committed mid-round (`fc17622`) to keep the tree clean for 10d's merge (lead-directed).
- **NEXT:** dispatch 10c (kernel-029) → 10d (author: energy emission + L21 scrub git-merge-cody + LESSONS §41→§51/§42→§52 reconcile + provider_call_failed) → 10e (kill/abort/drain/latching) → /phase-exit P2 + /phase-exit P3 close the track.

### Round-4 cont. (10c done; 10d/10e planning) — 2026-06-22
- **10c (kernel-029) — DONE `d369acb`.** Edges: partial-failure (running→degraded→verifying, minPopulationSurvival default 1) · all-fail→failed · zero-survivors (survivors:0) · degenerate reproduction (mutation_only) · **agenome.failed emission (FIRST emitter** of the kernel-026 sv5 event, active→failed, guard-validated). security-reviewer (invariant) PASS 0 findings. suite apps/api 211u. **FLAG #1 resolved (not a Finding):** no `generation.degraded` event needed — `degraded` is a TRANSIENT non-terminal; the partial-failure decision is a rule-#2-VALID PROJECTION off agenome.failed (authoritative), failed-IDs ride the generation.verifying marker as a single-writer derived convenience.
- **Carry-forward CONSUMED:** "agenome.failed emission = loop P3.10" → DONE (kernel-029).
- **NEW deferred carry-forward:** `candidate.generation_started` marker — the loop should emit it (§4, one of the 11 markers) before candidate.created, but 10b/10c don't. PURE observability (not rule-#2; candidate.created suffices for replay) → defer to an observability pass / fold into 10d's gateway-call touch. _(origin: kernel-029)_
- **CODY-BOUND ARCH note (→ lead, in addition to §5/§6 from kernel-028):** §5 — partial-failure degraded routing + minPopulationSurvival + degraded-as-projection (agenome.failed authoritative). _(origin: kernel-029)_
- **Lesson DECLINED:** kernel-029 flag-5 "degraded-as-projection" convention candidate — covered by LESSON §41 corollary 2 (intermediates separated from terminals, no event); no near-duplicate banked.
- **🔎 CODY STATE @ 10d planning (inspected from kernel worktree, `git log cody`):** cody HEAD = **`06299c9`** (scoped kernel-026 sv5 merge; 805ef9d ONLY — NOT my fc17622). cody has: `CURRENT_SCHEMA_VERSION=5`, event-type 41 members, **verifier scrub fix (§46)**, LESSONS **§37–50** (verifier §37–46 + kernel §47–50). **Demo round-4 (observability + reducer reconcile) has NOT landed on cody yet** — so 10d's `git merge cody` pulls the SMALLER base (sv5 [no-op for our contracts] + scrub fix [the L21 essential]), not demo observability. Fine — kernel energy build only needs the scrub fix; lead said "no need to time around me."
- **⚠️ 10d MERGE-RECONCILE PLAN (bake into the kernel-030 brief):**
  - CODE conflicts ≈ NONE expected (disjoint territories: verifier dirs + redaction.ts come in clean; contracts common via 805ef9d on both; IMPLEMENTATION_PLAN/ARCHITECTURE stale forks fast-forward to cody since track/kernel never edited them).
  - **DOC conflicts (orch-resolve, impl→orch handoff per L36): LESSONS.md + CLAUDE.md only.**
    - **CLAUDE.md:** KEEP track/kernel's **sv5 cross-doc rows** (RunEventType 41, schemaVersion 5) — cody's are STALE (37/4; the scoped merge didn't bring fc17622); adopt cody's lessons-index §37–50 + renumber track-local §41→**§51**, §42→**§52**; DROP track/kernel's dup index rows §37–40.
    - **LESSONS.md:** adopt cody's §1–50 (verifier §37–46 + kernel §47–50); add track-local §41→**§51** (terminal-audit) + §42→**§52** (loop-orchestration); DROP track/kernel's §37–40 (dupes of cody §47–50 — VERIFY identical first: cody §47=RNG/§48=caps/§49=energy/§50=preflight == track/kernel §37/38/39/40).
  - Sequence (L36): impl `git merge cody` → resolves any code conflicts + confirms suite green (markdown UU don't block tests) → HANDS LESSONS.md/CLAUDE.md UU to orch → orch resolves in-territory → impl commits the single merge commit → THEN the energy build.
- **P3.10 decomposition (UPDATED):** 10a✓ 10b✓ 10c✓ · **10d (kernel-030) = merge + energy** (Phase 1: git-merge-cody + doc-reconcile handoff [merge commit]; Phase 2: energy.spent success-only emission + provider_call_failed [no debit] + scrub→append→read round-trip on energy.spent ProviderMeta [feat commit]) [SAFETY rule#4/#8] · **10e (kernel-031) = kill/abort** (cap-breach incl. energyBudget enforcement via cumulativeSpend≥budget + wall-clock + operator-stop → planKillSwitch → drain-then-terminalize + latching halt + generation_failed-on-abort + partial summary) [SAFETY rule#1]. **energyBudget ENFORCEMENT is 10e** (10d only EMITS energy.spent; 10e folds cumulativeSpend→capLedger→breach→kill).

### Round-4 cont. (10d DONE; 10e next) — 2026-06-22
- **10d (kernel-030) — DONE, 2 commits:** merge **`d7fa661`** (cody@bae1842 — NOT the stale 06299c9; see below) + energy **`8b79888`** (feat(runtime)). Task #30 done. security-reviewer (invariant) PASS 0 findings. Suite: contracts 183 · observability 16 · apps/api 392u + 79i · apps/web 145.
- **⚠️ CODY ADVANCED MID-MERGE (handled):** the impl's first `git merge cody` captured cody@**06299c9**; cody had concurrently advanced to **`bae1842`** (demo round-4 sealed: sv5 projections/web status-map/`packages/observability`, /phase-exit P7 CLEAR, LESSONS §1–62). Caught it (cody ARCHITECTURE referenced LESSON §62 my §1–50 inspection lacked). Flagged lead → GO → impl aborted + re-merged **bae1842**. So the reconcile target moved §51/§52 → **§63/§64** (cody now to §62).
- **MERGE RECONCILE (orch-resolved, L36 handoff) — DONE:** LESSONS.md = cody §1–62 canonical + kernel §41→**§63** (terminal-audit) + §42→**§64** (loop-orch); DROPPED track §37–40 (≡ cody §47–50 dups). CLAUDE.md = cody §37–62 index + §63/§64; **KEPT track's sv5 cross-doc rows** (RunEventType 41 / sv5) over cody's STALE 37/sv4 (the scoped-merge doc-skew — banked §65). ARCHITECTURE.md + IMPLEMENTATION_PLAN.md = cody's authoritative (stale forks; empty diff vs cody post-merge). Verified: 0 markers, LESSONS §1–64 contiguous, CLAUDE index 64 rows no dups.
- **LESSONS banked this slice:** **§65** (scoped cross-track merge → integration mirror-docs stale vs contracts → back-merge reconciles per-field; extends §35/§36) + **§66** (redaction-test planted-secret fixture trips gitleaks on the var NAME → guard-clean fixture, never --no-verify). _(Note: track-local §63/§64/§65/§66 are above cody's §62 — no collision at the eventual kernel→cody merge.)_
- **SCRUB FIX location correction:** the verifier fix is in `packages/contracts/src/security/redaction.ts` (number/boolean exempt from sensitive-key whole-redaction), NOT `event-store/redaction.ts` (the brief's path was off — the append path composes the contracts scrub). energy.spent ProviderMeta tokensIn/tokensOut now survive the scrub as numbers (round-trip test pins it).
- **✅ P2.3/P2.8 UNBLOCKED** (lead heads-up): the bae1842 merge landed `packages/observability` in track/kernel. Sequence P2.3 (Langfuse correlation) + P2.8 after 10d / at the P2 phase-exit; **FLAG THE LEAD when slotting** (per the lead). _(carry-forward from §C, now unblocked)_
- **CODY-BOUND ARCH note (→ lead, +§5/§6 from kernel-028 + the kernel-029 §5):** §5 — the loop's energy.spent success-only emission (llm via reconcileEnergy+costMap, spawn/tool flat) + provider_call_failed per attempt (no debit) + the scrub round-trip. _(origin: kernel-030)_
- **DEFERRED → 10e (kernel-031):** energyBudget ENFORCEMENT (cumulativeSpend≥budget → energy_exhausted → drain) + kill/cap-breach/wall-clock/operator-stop abort → planKillSwitch → drain-then-terminalize the EXCLUDED states (completing→completed, stopping→stopped, degraded→verifying→failed) + LATCHING halt (kill stays armed through the drain) + generation_failed-on-abort + partial summary + the "provider failures exceed run retry cap" run-level branch. **DEFERRED beyond 10e:** candidate.generation_started marker (observability) · successor-population threading · real attemptFailures/tool-call gateway-surfacing impl (gateway wiring) · run-terminal classification (P3.11).
- **Context @ 10d done:** OK — max 64% (impl), climbing; 10e likely crosses WARN → ping lead on the crossing (cycle plausible before/after 10e).
