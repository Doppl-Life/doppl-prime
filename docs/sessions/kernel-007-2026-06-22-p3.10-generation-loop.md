# kernel-007 — P3.10 generation loop (sv5 amendment → skeleton → edges → energy → kill)

- **Date:** 2026-06-22
- **Phase:** Phase 3 (kernel runtime) — **P3.10 generation loop COMPLETE**
- **Predecessor:** [kernel-006](kernel-006-2026-06-21-runtime-safety-substrate.md)
- **Successor:** _(none yet)_

## Why this session existed

Resume from pause. The P3 substrate (P3.1/2/4/5/6/9 — guards, caps, kill switch, energy ledger, RNG, seed set, gateway, append path) was built + merged but had **no production caller**. This session built P3.10 — the generation loop that composes the substrate into its first real consumer — preceded by the kernel-026 sv4→5 terminal-event amendment the loop needed.

## What was built (7 implementer commits)

**Files created:**
- `apps/api/src/runtime/loop/generationLoop.ts` (kernel-028) — `runGenerationLoop` + runtime-local ports (`GenerationGateway`, verify/score/reproduce seams) + `transitionGenerationOrThrow`/`transitionAgenomeOrThrow` guards.
- `apps/api/src/runtime/loop/killDrain.ts` (kernel-031) — `executeKillAndDrain`: planKillSwitch → append §3-legal terminals → drain-then-terminalize the kill-EXCLUDED states under a latching halt.
- `apps/api/test/unit/runtime/loop/generationLoop.test.ts` — grew across 028–031 to 30 tests.

**Files modified:**
- `packages/contracts/src/events/event-type.ts` + `version.ts` (kernel-026) — `RunEventType` 37→**41** (+run.cancelled/generation.skipped/agenome.failed/candidate.rejected), `CURRENT_SCHEMA_VERSION` 4→**5**.
- `apps/api/src/runtime/caps/killSwitch.ts` (kernel-026) — `runEventFor` names run.cancelled/generation.skipped (were null).
- `apps/api/src/runtime/energy/costMap.ts` + `config/configSchema.ts` + `config/loadConfig.ts` (kernel-027) — `CostMapConfigSchema` (z.infer single-source) + `AppConfig.costMap` (defaults<file).
- `generationLoop.ts` extended: 028 skeleton (lifecycle + markers + tool relay + agenome.spawned + candidate.created via append path; injected seams consumed as DATA) → 029 edges (partial-failure/degraded, zero-survivors, degenerate reproduction, agenome.failed, minPopulationSurvival) → 030 energy (energy.spent llm/spawn/tool success-only + provider_call_failed) → 031 kill (full cap-set detection + boundary kill + latching break + killSummary).
- `apps/api/src/runtime/index.ts` — barrel exports for the loop + killDrain.

**Commits:** 805ef9d (026) · e88bf6b (027) · 6279526 (028) · d369acb (029) · d7fa661 merge + 8b79888 (030) · 201dfe3 (031).

## Decisions made

- **kernel-026 sv5 amendment** (§19 playbook, user-ratified): the 4 reachable §3 terminals were unrepresentable; added them so every terminal is rule-#2 replayable. Additive/backward-compat.
- **Option-b loop** (lead-confirmed): verify/score/reproduce are INJECTED seam ports; the loop consumes their events as DATA (readByRun) and NEVER authors a seam-owned type (selection P5 not in cody, verifier evidence-only).
- **Runtime-local `GenerationGateway`** (D2): composes the frozen `ModelGateway`; tool calls + attemptFailures ride alongside (the frozen `ModelGatewayResponse` can't carry observability) — real surfacing deferred to gateway wiring.
- **Partial-failure recording** (029): `agenome.failed` is the authoritative per-agenome record; the degraded fact is a derived denormalization on the `generation.verifying` marker (no `generation.degraded` event needed — transient, re-derivable). Confirmed not-a-Finding.
- **Energy** (030): llm `actual` from REAL providerMeta (never the estimate, rule #8); failed call → provider_call_failed + agenome.failed, NO debit. The cody merge brought the verifier scrub fix (number-exempt) so ProviderMeta tokensIn/tokensOut survive the persistence scrub as numbers.
- **Kill** (031): LATCHING halt — `executeKillAndDrain` terminalizes every non-terminal (plan + drain of excluded completing/stopping/degraded); a drained transient never re-arms. `energy_exhausted` is the named registry terminal for an energyBudget breach.

## Decisions explicitly NOT made (deferred)

- **kernel→cody merge** — deferred to track-completion (lead's call); cody-bound §5/§6 arch notes parked in ledger §I.
- **run-terminal classification** (completed-vs-failed verdict) = P3.11; **worker** = P3.12; **crash-forward** = P3.13.
- **Per-agenome/mid-stage kill responsiveness** — the loop checks at GENERATION boundaries for MVP; `executeKillAndDrain` already handles a mid-stage generation (only the check cadence is boundary-granular). P3.12/future.
- **candidate.generation_started marker** + **successor-population threading** (gen-0 persists) — deferred.

## TDD compliance

**Clean — all slices test-first** (RED → Step-2.5 → GREEN). kernel-026 used the §19 set-new-expectation-against-old-contract RED. kernel-027 caught + strengthened a vacuous-pass RED (`Object.isFrozen(undefined)===true`) before implementing (LESSON 10). The 10b `operation_markers` `not.toContain('energy.spent')` assertion was a phase-scope statement retired in 10d (energy.spent now emitted) — orch-confirmed, not a violation. No safety-critical TDD skips.

## Cross-doc invariant audit (multi-track — memory check)

Only Appendix-A field change this session = **kernel-026** (`RunEventType` 37→41, `CURRENT_SCHEMA_VERSION` 4→5) — flagged at Step 9; orchestrator wrote the `apps/api/CLAUDE.md` rows; `ARCHITECTURE.md` Appendix-A is cody-bound (lead). kernel-027–031 introduced only runtime-local shapes (`CostMapConfig`, `minPopulationSurvival`, `GenerationGateway`/seam ports, `killDrain`) — NO Appendix-A rows (confirmed at each Step 9). No drift.

## Reachability

- **kernel-026 registry/sv5:** reachable via the append-path validation (`AppendInput` = RunEventEnvelope.omit) + the killSwitch.
- **kernel-027 costMap:** reachable via `loadConfig`; consumer = the P3.10 loop (now CONSUMED in 10d energy).
- **kernel-028–031 loop:** `runGenerationLoop` reachable via the runtime barrel; it is the substrate's first real consumer (materializeGen0 / guards / caps / RNG / energy / killSwitch all now reached). `planKillSwitch` (P3.4) got its FIRST production caller (`executeKillAndDrain`). Named production caller of the loop = the P3.12 worker (deferred — tested-but-not-yet-worker-wired, expected substrate).

## Open follow-ups

- **Deferred consumers / TODOs (ledger):** P3.11 run-terminal · P3.12 worker (loop caller) · P3.13 crash-forward · per-agenome mid-stage kill cadence · candidate.generation_started · successor-population threading · real GenerationGateway impl surfacing tool calls/attemptFailures · `KillTrigger.wall_clock` dead-variant prune (low-pri).
- **Cross-track:** kernel→cody merge deferred to track-completion; §5/§6 arch notes (loop orchestration boundary, loop↔gateway tool-call seam, energy emission, kill execution) parked in ledger §I for the merge. **P2.3/P2.8 now unblocked** (observability merged in 10d).

## How to use what was built

`runGenerationLoop(deps)` (deps: runId, config, eventStore, gateway, seams, optional minPopulationSurvival/now/operatorStop) drives bounded generations through the §3 lifecycle, appends only kernel-owned events, delegates verify/score/reproduce to injected seams, debits success-only energy, and enforces caps via a latching kill. The P3.12 worker is its production caller.
