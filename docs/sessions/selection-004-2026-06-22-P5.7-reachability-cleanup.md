# selection-004 — P5.7 reachability cleanup (remove dead exports; /phase-exit P5 true-CLEAR)

- **Date:** 2026-06-22
- **Phase:** Phase 5 (selection track) — phase-exit gate cleanup
- **Track:** selection
- **Predecessor:** [selection-003](selection-003-2026-06-22-phase5-selection-wiring.md) (Phase-5 selection wiring W1→W3b-2c)
- **Successor:** _(none expected — Phase 5 closes after the orchestrator's `/phase-exit P5` seal; selection track stands down)_
- **Brief:** `docs/briefs/selection-018-P5.7-reachability-cleanup.md` (spec-lint PASS @796eea05)
- **Slice commit:** `f07367d`

## Why this session existed

P5 selection wiring was COMPLETE + green (selection-003, last slice W3b-2c `beb36b2`), but `/phase-exit P5` was BLOCKED on a single row: the reachability auditor (`docs/audits/P5-reachability.md`) flagged 4 unreachable barrel exports. The human chose a **true-CLEAR cleanup slice over a waiver**: remove the 2 genuinely-dead exports and record the 2 accepted Phase-D/demo deferrals — so the auditor re-run returns 0-unreachable legitimately.

## What was built

This is a **removal/cleanup slice** — no new behavior. The "RED" was suite-green-after-removal + the orchestrator's reachability re-run.

### Files deleted
- `apps/api/src/selection/parent-selection.ts` — `selectParents` (+ `SelectParentsInput`/`SelectParentsResult`). Superseded + test-only (grep-confirmed no production caller).
- `apps/api/test/unit/selection/parent-selection.test.ts` — its unit test.

### Files modified
- `apps/api/src/selection/index.ts` — dropped the `selectParents`/`SelectParents*` barrel re-exports (lines 55–56) + dropped `jaccardSimilarity` from the lexical-fallback barrel line (kept `lexicalNoveltyScore`).
- `apps/api/src/selection/seams/score-seam.ts` — **CQ-2:** added a WHY comment on the pre-loop `readByRun` snapshot (the `novelty.scored` events this seam emits during the loop are intentionally NOT in `rows`; the cosine comparison set accumulates in-seam, so a future component must not read this generation's novelty from `rows`).
- `apps/api/test/integration/selection/successor-threading.test.ts` — **CQ-1:** message-pinned the bare `rejects.toThrow()` to `toThrow(/cannot derive the next generationId/)` (LESSONS §10 — a bare `toThrow` false-passes on any throw; the regex matches the actual fail-loud message in `successor-threading.ts:33`).

## Decisions made

- **DELETE `selectParents` (file + test) rather than un-export it.** It is superseded by the wired path (the kernel loop's `resolveEligibleParents` eligible-set + selection's `allocation` fitness-ranked distribution; §8's deterministic tie-break is satisfied by the wired path's id-order) and has no production caller. Deletion is the cleanest route to 0-unreachable; un-exporting would leave the auditor potentially still flagging it. (Orchestrator APPROVED at Step 2.5.)
- **`jaccardSimilarity`: barrel-removal only, keep the module export.** `lexicalNoveltyScore` (a reachable production function) calls it internally → it stays reachable via that caller. The auditor walks the barrel surface, so dropping the redundant barrel re-export resolves the flag. Its unit test imports it from the module path directly (`../novelty/lexical-fallback`), not the barrel, so it stays green with zero test edit.

## Decisions explicitly NOT made

- **`createStartRun` + `noveltyScoreOf` — KEPT untouched (accepted reachability deferrals, named consumers).** Not cleaned. `createStartRun` is the Phase-D production `main.ts` boot wiring (e2e-proven via `test/integration/routes/runs-execution.e2e.test.ts`); `noveltyScoreOf` is the rule-#7 replay-recompute helper for the demo/PD replay path. These are accepted-with-named-consumers, not dead code.
- **The other 4 P5-quality findings** (O(N×M) scan, `GENERATION_ID_PATTERN` coupling, `startRun` safeParse-fallback, MVP cull policy) — documented follow-ups, explicitly out of scope for this slice (brief Step-2.5 note 3).

## TDD compliance

**Clean.** Removal slice — nothing implemented before a test. The single test edit (successor-threading message-pin) tightens an existing assertion to pin pre-existing fail-loud behavior (LESSONS §10), an improvement, not a behavior change. No safety-invariant work (Step-8 review correctly skipped per policy — `security-reviewer: invariant` not triggered; `code-quality-reviewer: phase-boundary` is `/phase-exit`'s).

## Cross-doc invariant audit

**No model field changed** this session (no `@doppl/contracts` model touched). Multi-track memory check: nothing to flag at Step 9; no `ARCHITECTURE.md` edit owed.

## Reachability

- **§8 wired path UNCHANGED** — `cull` / `allocation` / `successor` / the 3 seams (`createScoreSeam`/`createReproduceSeam`/`createSuccessorThreading`) / the loop's private `resolveEligibleParents` were all untouched. Removing the unwired `selectParents` loses no §8 parent-selection behavior.
- **Removed exports:** `selectParents` (deleted), `jaccardSimilarity` (barrel-removed; still reachable via `lexicalNoveltyScore`). Expected auditor re-run result: 0-unreachable, with the 2 deferrals accepted.
- **Accepted tested-but-deferred (named consumers, not gaps):** `createStartRun` → Phase-D `main.ts` boot; `noveltyScoreOf` → demo/PD replay path.

## Open follow-ups

Step-9 categorized list (already routed hot to the orchestrator):
- **Cross-doc invariant changes:** none.
- **Findings:** none.
- **New deferrals:** none.
- **New wiring:** none (removal slice).
- **Accepted deferrals (orchestrator records at seal):** `createStartRun` (Phase-D boot wiring) + `noveltyScoreOf` (demo/PD replay helper) — accepted-with-named-consumers.
- **Next (orchestrator-owned):** reachability auditor re-run (expect 0-unreachable) → `/phase-exit P5` CLEAR → seal. Then the selection track stands down.

## Suite

unit 603/603 (87 files) · integration 125/125 (27 files) · typecheck / lint / format:check all clean (full `/preflight` gate).
