# Doppl `apps/api/` — Build Guide

> **You're in `apps/api/`.** This file plus root `CLAUDE.md` both load. The root file covers global project conventions + shared comm rules (track-prefix, escalation taxonomy, messaging budget); this file owns code-area conventions for the backend (Doppl kernel + subsystems).

## Launch protocol

| Working on... | cwd | Loads |
|---|---|---|
| Planning / docs / commits | repo root (`Capstone/`) | root `CLAUDE.md` only |
| the backend (Doppl kernel + subsystems) code | `apps/api/` | this `CLAUDE.md` + root |
| the React dashboard code | `apps/web/` | `apps/web/CLAUDE.md` + root |

If you find yourself fighting the wrong conventions, check your cwd.

## Session start/end protocol

**At session start:**
1. Read `IMPLEMENTATION_PLAN.md` (repo root) **by section, not whole** — `grep -n "^##" IMPLEMENTATION_PLAN.md` for offsets, then Read with offset/limit just "Currently in progress" + the active phase. (The file grows; never load it whole.)
2. Confirm with the user what feature this session is targeting.
3. Read the relevant section of `ARCHITECTURE.md` from the lookup table below.

**At session end** (only when the user explicitly says we're done):

1. **Implementer runs `/session-end`.** Implementer writes ONLY:
   - `apps/api/` code files (the slice's implementation)
   - test files (the slice's tests)
   - dependency manifest / lockfile (deps the slice adds)
   - `docs/sessions/<NNN>-<date>-<topic>.md` (session doc, created at `/session-end` Step 5)

   **Implementer must NOT touch (all orchestrator territory).** *This list is the canonical statement
   of the territory rule — `/session-end`, the brief template, and the generated
   `scripts/guards/territory-guard.sh` PreToolUse hook (which mechanically enforces it in team mode)
   all point here.*
   - `IMPLEMENTATION_PLAN.md`
   - `apps/api/LESSONS.md`
   - `apps/api/CLAUDE.md` (entire file — both the Cross-doc invariants table AND the Lessons logged index)
   - `ARCHITECTURE.md`
   - `docs/orchestrator-briefing.md` / `docs/tdd-brief-template.md` / `docs/briefs/` / `docs/runbooks/` / `docs/audits/` _(NOT `docs/sessions/` — implementer's; NOT `docs/team-handoffs/` — lead's)_
   - other top-level deliverable / design docs
   - `.gitignore` and root-level dotfiles (unless adding a new artifact to ignore, flagged at Step 9)

   At Step 10: **explicit `git add <path>` per slice file; never `git add -A`/`.`; never stage an orchestrator-territory file.** Changes to any orchestrator-territory file (a new cross-doc model, a lesson, an arch note) are **flagged at Step 9**, not edited here — the orchestrator writes them hot (root `CLAUDE.md` + the Step-9 matrix).

2. **Orchestrator runs `/orchestrate-end`** for round close-out + Carry-forward triage + round terminal commit + push.

## Lookup table — where to find canonical info

Don't paste these sections into the prompt. Grep the file:section, read only what you need. `/check-arch <topic>` dispatches off this table.

| Topic | File (relative to repo root) | Section |
|---|---|---|
| Contracts & event model (RunEventEnvelope, RunEventType, energy unit, replay/RNG) | `ARCHITECTURE.md` | §4 |
| Runtime kernel (state machines, caps, energy ledger, worker, crash-forward) | `ARCHITECTURE.md` | §5 |
| Model gateway & provider integration (registry, OpenRouter, embeddings, retrieval) | `ARCHITECTURE.md` | §6 |
| Verifier council & checks (held-out judge, rotation, allowlist, injection isolation) | `ARCHITECTURE.md` | §7 |
| Selection, scoring & reproduction (fitness, novelty, fusion, mutation) | `ARCHITECTURE.md` | §8 |
| Persistence & projections (Postgres, migrations, replay reader, embeddings) | `ARCHITECTURE.md` | §9 |
| Lessons logged (full prose) | `apps/api/LESSONS.md` | by lesson # |

<!-- Starts near-empty. Add a row whenever a topic is looked up twice. (Seeded with the backend's load-bearing § anchors — this area touches most subsystems.) -->

**Code intelligence & docs (when available):** prefer a code-intelligence MCP / docs MCP over grep+read loops — see root `CLAUDE.md` "Code intelligence & docs."

## Stack

<!-- ▼ EXAMPLE BLOCK [id=area-stack]: stack quick-reference for implementer sessions. Canonical stack lives in root CLAUDE.md + ARCHITECTURE.md; this is the cheat sheet. ▼ -->

- **Runtime:** Node 22 LTS (pnpm workspace)
- **Framework:** Fastify (REST commands/queries + SSE run-event stream)
- **Validation:** Zod (shared schemas from `packages/contracts`; `z.infer` for types)
- **Persistence:** Drizzle + Postgres (append-only `run_events`; pgvector optional)
- **Lint / types / tests:** ESLint / `tsc --noEmit` (strict) / Vitest (unit + integration against a real Postgres)

<!-- ▲ END EXAMPLE BLOCK [id=area-stack] ▲ -->

## Standard commands

```bash
# Install deps (run once; re-run when the manifest changes)
pnpm install

# Run the dev server (if applicable)
pnpm dev

# Tests
pnpm test

# Quality
pnpm lint
pnpm format:check
pnpm typecheck

# Preflight (use before saying "done" with a feature)
pnpm lint && pnpm typecheck && pnpm test
```

## TDD protocol

**Write the failing test first.** Applies to deterministic code — see the TDD posture in root `CLAUDE.md` for what is test-first vs. exempt (the LLM-driven generation/critics/judge are eval-tested via `/eval`, not `/tdd`).

**Commit per slice when practical.** Never bundle a safety-critical slice with anything else.

## Forbidden patterns

<!-- ▼ EXAMPLE BLOCK [id=forbidden-patterns]: forbidden patterns — 3-5 narrow, enforceable, domain-specific rules. Shape: "Don't <pattern X> because <reason / past incident>; use <alternative Y>." Test-pin them where possible. Starts small; accretes as lessons surface. ▼ -->

Do not:

1. **Write code without a failing test first** (for deterministic code). Even one-line functions.
2. **Import a provider SDK (openai, @anthropic, openrouter, …) into a domain/runtime module** — vendor-couples the kernel, breaks replay, and is untestable; route through the `ModelGateway` port (safety rule 9).
3. **Enforce a cap or permission in prompt text** — a prompt can be ignored or injected; caps are kernel invariants enforced in the runtime (safety rule 1).
4. **Write to `run_events` outside the append-only writer** — bypasses the per-run `sequence`, the redaction scrub, and schema validation (safety rules 2, 4).
5. **Re-call a model / embedding / web provider on the replay path** — persist the outcome at run time; replay reads it (safety rule 7).
6. **Treat a projection as authoritative** — projections are derived; write the event, then rebuild the projection (safety rule 2).

**Enforcement patterns (machine-readable — `/preflight` warn-greps the staged diff against these).**
One `grep -E` (or `ast-grep`) expression per line, each tied to a numbered rule above. Rules that can't
be expressed as a pattern carry a `pin:` (test ref) or `accepted:` note on the rule itself instead.

```forbidden-patterns
# rule 2 (no provider SDK in domain/runtime): from ['"](openai|@anthropic-ai|openrouter)
# rule 4 (no raw event-table writes): (insert|update|delete).*run_events
# rule 1: pin: every slice opens with a failing test (Step 3); enforced by /tdd, not grep
# rule 3: pin: cap-enforcement tests in apps/api/test (kernel rejects over-cap spawn)
# lesson 14 (pinned-binary checks, not npx): npx\s+(prettier|eslint|tsc)
# lesson 100 (schemaVersion pin lives in 4 test spots): pin: grep CURRENT_SCHEMA_VERSION + toBe(<old>) across packages/contracts/test before a schemaVersion bump
```

<!-- ▲ END EXAMPLE BLOCK [id=forbidden-patterns] ▲ -->

## Cross-doc invariants — schema/docs mirroring

Several typed models in this codebase are **contracts** mirrored in `ARCHITECTURE.md` and indexed in the table below. The architecture doc is the canonical contract; the model is the executable enforcement. Drift produces silent disagreement.

**Downstream tracks (P1–P7) validate against the P0.14 contract-test surface** — import `CANONICAL_FIXTURES` (one valid fixture per Appendix-A model) + `objectFieldNames` / `FIELD_SET_SNAPSHOTS` from `@doppl/contracts` for consumer/producer agreement tests; never redefine a shape (single import boundary, lesson §5).

**Authoring discipline (orchestrator owns this table).** The implementer never edits this table or `ARCHITECTURE.md` directly — it flags a field add/remove/rename at Step 9 as a `Cross-doc invariant change`; the orchestrator writes the row + the arch edit hot the same round (see root `CLAUDE.md` + `docs/orchestrator-briefing.md`). Commits stagger; the working tree stays aligned within the round.

| Model | `ARCHITECTURE.md` section | Notes |
|---|---|---|
| `RunEventEnvelope` | §4 | Strict 14-field envelope; `actor` = closed 7-role union (operator/runtime/agenome/critic/check_runner/selection_controller/system); `sequence` sole ordering key; `occurredAt` display-only; generic object `payload` (P0.10 narrows per-type); `schemaVersion` + exported `CURRENT_SCHEMA_VERSION=8` (linearized bumps: P0.1-amend 1→2, judge-output 2→3 [P0.16], P0.15-amend+P0.5-amend 3→4, terminal-events 4→5 [kernel-026, P0.1-amend], FB.0 frontend-v2 run-controls 5→6 [RunConfig +generationOperators?/generationBias?/modelRouteOverride? + the closed GenerationOperator enum], FB.6 frontend-v2 raw-reasoning telemetry 6→7 [new RunEventType llm_call_telemetry + high-traffic LlmCallTelemetry], FB.4 frontend-v2 diverge/converge dial 7→8 [optional samplingParams{temperature?} on ModelGatewayRequest + LlmCallTelemetry]; readers accept all `schemaVersion ≤ current`, so v1–v7 envelopes still validate — additive/backward-compatible). Frozen in `packages/contracts` (P0.1); enforced by field-name schema-snapshot (`spec(§4)`). |
| `RunEventType` | §4 | Closed **41-member** registry: the **20 lifecycle** (incl. `run.cancelled` + `generation.skipped` — kernel-026 sv5) + **9 failure/terminal types** (RISK-006; incl. `agenome.failed` + `candidate.rejected` — kernel-026 sv5) + **11 operation-start / in-flight observability markers** (P0.1-amend — generation.verifying/scoring/reproducing, candidate.generation_started, critic.review_started, check.started, novelty.scoring_started, judge.review_started, fusion.started, tool_call.started/finished; persisted + replay-faithful + **no energy debit** rule #8 — they fall back to the generic payload, never narrow to EnergyEvent) + the **terminal `judge.reviewed`** (P0.16 judge-output amendment — the held-out-judge acceptance result; narrows to `JudgeResult`, the §2.5 verifier→selection seam; the terminal half of the `judge.review_started` marker, NOT itself a marker); rejects unlisted values. The **4 sv5 terminals close the rule-#2 gap** (every reachable §3 terminal is now persistable/replayable) — **registry-add only**: `run.cancelled`/`generation.skipped` named by the P3.4 killSwitch + emitted by the P3.10 loop; `agenome.failed` emitted by the P3.10 loop (active→failed); `candidate.rejected` emitted by the RUNTIME on SELECTION's verdict (P3↔P5 seam, MVP defined-but-not-yet-emitted; verifier evidence-only/rule #6 + retired — no cross-track blessing gate). Frozen in `packages/contracts` (P0.1 + P0.1-amend + P0.16 + kernel-026 terminal-events sv5); member-set snapshot (`spec(§4)`). |
| `RunConfig` / `RunCaps` | §4, §5 | `RunCaps` = strict 6 positive-int caps (maxPopulation/maxGenerations/energyBudget[doppl_energy]/maxSpawnDepth/maxToolCalls/wallClockTimeoutMs); `RunConfig` = seed, enabledSubtypes[≥1], caps, modelProfile, scoringPolicyVersion, rngSeed (required, §4 replay) **+ FB.0 (sv6) 3 OPTIONAL run-controls: `generationOperators?`(array of closed 7-member `GenerationOperator` mutagen enum) / `generationBias?`(number∈[−1,+1], 0 neutral) / `modelRouteOverride?`(`partialRecord(ModelRole, strict {provider,modelId})`)** — operators/bias are GENERATION inputs (rule #5 DATA, rule-#6-safe), modelRouteOverride is runtime allowlist-clamped (FB.2; contract fixes shape only); `ScoringPolicy`/`FinalJudgeRubric`/`FinalJudgeAxis` UNCHANGED (rule #6, asserted). Closed `Subtype` union (`cross_domain_transfer`\|`zeitgeist_synthesis`) defined canonically in P0.3 (`src/domain/subtype.ts`) for P0.5 to import. Pure `validateRunConfig` (defaults<file<env deep-merge, fail-fast) accepts the new optional fields + rejects an out-of-range bias / unknown operator at boot. Frozen `packages/contracts` (P0.3 + FB.0); field/member schema-snapshot (`spec(§4)`). |
| `Agenome` | §3 | Strict 11-field agent-genome (id, runId, generationId, parentIds[], systemPrompt, personaWeights, toolPermissions[], decompositionPolicy, spawnBudget, mutationMeta?, status) + closed 7-state `AgenomeStatus` (seeded/active/spent/eligible_parent/failed/reproduced/culled). Schema encodes SHAPE only — parentIds 0–2 + spawnBudget clamp are kernel-enforced (P3), not the contract. Frozen `packages/contracts` (P0.4); field/status schema-snapshot (`spec(§3)`). |
| `CandidateIdea` | §3 | Subtype-discriminated 11-field unit-of-work (id, runId, generationId, agenomeId, subtype, title, summary, claims[], evidenceRefs[], status, subtypePayload) via `z.discriminatedUnion('subtype',[cdt,zeit])` — correlation structural (lesson §7); closed **9-state** `CandidateStatus` (created/**repairing**/under_review/checked/scored/selected/rejected/culled/invalid — `repairing` added P0.5-amend for the §3 `created→repairing→under_review`/`repairing→invalid` repair edge); `subtype` imported from P0.3 `Subtype` (not redefined, §5); `claims[]` permissive on count (≥1 is a kernel rule, §6 — empty array parses, empty-string element rejected). Frozen `packages/contracts` (P0.5); per-variant field-set + status schema-snapshot (`spec(§3)`). |
| `CrossDomainTransferPayload` / `ZeitgeistSynthesisPayload` | §3 / DATA_MODEL.md | Strict subtype payloads: CDT = sourceDomain/sourceTechnique/targetDomain/targetProblem/transferMapping/expectedMechanism (+ `executableCheckIdea?`); Zeit = thesis/audience/currentSignals[]/whyNow/falsifiablePredictions[]/comparablePriorArt[]. String fields + array elements `.min(1)`; empty arrays OK. Frozen `packages/contracts` (P0.5); field-set schema-snapshot (`spec(§3)`). |
| `EvidenceRef` | §4 | Strict ref: closed 6-kind `EvidenceKind` (trace/check_output/prior_art/signal/raw_output/other) + all-optional `.min(1)` pointers (eventId?/uri?/label?/langfuseObservationId?). Resolves WITHIN the Postgres tier — *resolution* is the P1.7 resolver's job, not the schema (§9, lesson §6). Consumed by P0.6 `CriticReview.evidenceRefs[]` + P0.7 `CheckResult.evidenceRefs[]`. Frozen `packages/contracts` (P0.5); field-set + kind schema-snapshot (`spec(§4)`). |
| `CriticReview` / `CriticMandate` | §7 | Strict 7-field review (id, candidateId, mandate, scores{name→number}, critique, confidence∈[0,1], evidenceRefs[] of `EvidenceRef`) + closed 5-member `CriticMandate` (factual_grounding/novelty_prior_art/feasibility/falsification/subtype_specific). **Emit-only (safety rule #6):** NO winner/selected/scoreOverride/policyVersion field is representable — pinned by strict + 7-field snapshot (lesson §9, anti-reward-hacking). Frozen `packages/contracts` (P0.6); field/member schema-snapshot (`spec(§7)`). |
| `criticInput` (+ `CRITIC_INPUT_SENTINEL`, `wrapUntrusted`) | §14 | Injection-isolation shape (safety rule #5): strict 2-field — trusted `rubric` ({mandate, instructions}) vs untrusted `candidate` (string) as DISTINCT fields so candidate text is data, never instructions (T-002/RISK-008). `wrapUntrusted(text)` bounds the candidate with the exported `CRITIC_INPUT_SENTINEL` AND neutralizes embedded sentinels (output has it exactly twice for any input; single-pass-complete, lesson §8). Per-call nonce delimiter = P4 future hardening (non-pure). Frozen `packages/contracts` (P0.6); field-set + rubric sub-shape + sentinel-value snapshot (`spec(§14)`). |
| `CheckResult` / `CheckRunnerAdapter` | §7 | `CheckResult` = strict 9-field (id, candidateId, checkType[open string], status, score?, output?, skipReason?, evidenceRefs[] of `EvidenceRef`, error?); closed 3-state `CheckStatus` (passed/failed/skipped); `skipReason` present IFF skipped. `CheckRunnerAdapter` = allowlist-registry descriptor `{id, checkType, subtype?, label?}`, **non-executing by shape** (no code-carrying field representable — rule #3, lesson §11). `resolveCheckAdapter(registry, req)` fails safe to a `skipped` CheckResult on an unregistered id (own-property lookup; never executes). Frozen `packages/contracts` (P0.7); field/status schema-snapshot (`spec(§7)`). |
| `NoveltyScore` | §8 | Strict 9-field (id, candidateId, vector, embeddingModelId, dimension, comparisonSet, method, score, explanation). **Rule #7 (replay):** `vector`(array&lt;number&gt;) + `embeddingModelId` + `dimension`(int&gt;0) all REQUIRED → replay reads the persisted vector, never re-embeds (lesson §13). `method` = OPEN string (no closed enum); `comparisonSet` = array&lt;string&gt;; `score` permissive number; `length===dimension` is a kernel check (§6). Frozen `packages/contracts` (P0.8); field-set + `vector`-present snapshot (`spec(§8)`). |
| `FitnessScore` / `ScoringPolicy` | §8 | `FitnessScore` = strict 6-field (id, candidateId, total, components, policyVersion, explanation); **rule #6:** `policyVersion` REQUIRED + identically typed to `ScoringPolicy.version` → each score bound to its exact policy (immutability-via-versioning, lesson §12). `components` = OPEN name→number record (decomposed signals, §8 explainability). `ScoringPolicy` = strict {version, weights, normalization?}; `weights` = OPEN name→number record — STRUCTURE frozen, weight VALUES deferred-open (the only deferred-open contract values). Frozen `packages/contracts` (P0.8); field-set + `policyVersion`-present snapshot (`spec(§8)`). |
| `EnergyEvent` | §4, §5 | Strict 10-field (id, runId, generationId?, agenomeId?, eventType, estimate, actual, unit, reason, providerMeta?). **Rule #8 (success-only):** `eventType` closed `llm`/`tool`/`spawn` (no failure member); `estimate`+`actual` both REQUIRED; `unit`=`z.literal('doppl_energy')`; NO failed/retried/repaired/success field representable (strict + field-set snapshot incl. not-contains, lesson §9). estimate/actual int (sign=kernel); `providerMeta?` = shared `ProviderMeta`. Frozen `packages/contracts` (P0.9); field/eventType snapshot (`spec(§4)`). |
| `ReproductionEvent` | §8 | Strict 7-field (id, runId, parentAgenomeIds[], childAgenomeId, mode, crossoverPoints, mutationSummary). **Rule #7 (replay):** `crossoverPoints`(`int[]`) + `mutationSummary`(`record<string, string\|number\|boolean>`) both REQUIRED persisted RNG outcomes → replay reconstructs, never re-samples (lesson §13). `mode` closed `fusion`/`crossover`/`output_synthesis`/`mutation_only`; parentAgenomeIds count 0–2 = kernel (§6). Frozen `packages/contracts` (P0.9); field/mode snapshot (`spec(§8)`). |
| `ProviderMeta` | §6 | Shared provider-call metadata `{provider, modelId, gatewayRequestId, tokensIn:int≥0, tokensOut:int≥0, costEstimate?:number}`; NO credential field (env-only, §14). Canonical in `src/gateway/provider-meta.ts` (P0.9, first consumer = `EnergyEvent.providerMeta?`); **P0.12's `ModelGatewayResponse.providerMeta` imports it, never redefines** (lesson §5). Frozen `packages/contracts` (P0.9); field-set snapshot (`spec(§6)`). |
| `ModelRole` / `ProviderCapability` / `ModelRoute` | §6 | `ModelRole` closed 7-union (population_generator/critic/subtype_check/embedding/final_judge/fusion_synthesis/retrieval). `ProviderCapability` strict {structuredOutputs, embeddings (both required bools), toolCalling?, streaming?}. `ModelRoute` strict {role, provider, modelId, capability, fallbackRouteIds[] (may be empty)} — does NOT force a single provider (embedding=OpenAI + critic=OpenRouter both valid). Frozen `packages/contracts` (P0.11); field/role snapshot (`spec(§6)`). |
| `ModelGatewayRequest` / `ModelGatewayResponse` | §6 | The ONLY provider seam domain code sees (no vendor SDK leak, §2.5). `Request` {role(ModelRole), prompt XOR messages, schema?, maxTokens?} — messages carry closed `ChatRole` (system/user/assistant); exactly-one-of prompt/messages. `Response` {accepted, output?, validationResult, providerMeta(shared P0.9 `ProviderMeta`), langfuseTraceId?, rejection?} — `validationResult` closed `accepted\|repaired\|rejected`, `accepted ⇔ result≠rejected`, `rejection` present IFF rejected. NO credential field (rule #4/§14). `output?`/`schema?` opaque `z.unknown()` — the PERSISTENCE boundary scrubs (P0.2), not the contract. Frozen `packages/contracts` (P0.12); field/validationResult/ChatRole snapshot (`spec(§6)`). |
| per-type payload map (`events/payload-map.ts`) | §4 | Narrowing layer OVER the generic frozen `RunEventEnvelope.payload` — does NOT mutate the envelope. `HIGH_TRAFFIC_PAYLOAD_MAP: Partial<Record<RunEventType, ZodType>>` maps the 7 §4 high-traffic types to their frozen model (energy.spent←EnergyEvent, candidate.created←CandidateIdea, critic.reviewed←CriticReview, check.completed←CheckResult, novelty.scored←NoveltyScore, fitness.scored←FitnessScore, judge.reviewed←JudgeResult — same schema validates write + model). `resolvePayloadSchema(type)` = own-property lookup (lesson §11), fails OPEN to `GENERIC_PAYLOAD_SCHEMA` (= envelope's `z.record(z.string(),z.unknown())`) for non-high-traffic types, fails CLOSED (reject) on a high-traffic mismatch. `enforcePayloadCeiling(payload)` = bounded payload-DoS primitive: **depth-first (iterative DFS, early-exit) THEN size** (`MAX_PAYLOAD_BYTES`=1 MiB, `MAX_PAYLOAD_DEPTH`=32, literal-value-pinned), result-object, never throws (unserializable→`max_bytes`). `validateEventPayload(type,payload)` composes both for the P1 append path. `fitness.scored`↔novelty link = `candidateId` + `components.novelty` (Q1=A; frozen `FitnessScore` unchanged, `noveltyScoreId` strict-rejected). Frozen `packages/contracts` (P0.10 + P0.16 judge.reviewed; the 4 kernel-026 sv5 terminals are low-traffic → no `HIGH_TRAFFIC_PAYLOAD_MAP` entry, generic JSONB payload, map unchanged at 7); key-set + per-key mapping + literal-constant schema-snapshot (`spec(§4)`). |
| `Run` / `Generation` (+ `RunStatus`, `GenerationStatus`) | §3 | `Run` = strict 7-field {id, seed, enabledSubtypes[], caps:RunCaps, status, startedAt, completedAt?}. `seed` = run/problem-scenario string (= `RunConfig.seed` by name, lesson §5; **DISTINCT from the RNG seed** `RunConfig.rngSeed`:int). `enabledSubtypes` = `z.array(Subtype)` with **NO `.min(1)`** (count ≥1 is a kernel rule §6 — `RunConfig.enabledSubtypes` carries the boot-gate `.min(1)`, the entity does not). `caps` imports frozen `RunCaps` (P0.3). `RunStatus` = closed 8 (configured/running/completing/completed/stopping/stopped/failed/cancelled). `Generation` = strict 6-field {id, runId, index(int≥0), status, startedAt, completedAt?}; `GenerationStatus` = closed **9** (pending/running/**degraded**/verifying/scoring/reproducing/completed/failed/skipped — `degraded` added P0.15-amend for the §3 `running→degraded→verifying` partial-failure edge). Frozen `packages/contracts` (P0.15 + P0.15-amend); field/status schema-snapshot (`spec(§3)`). |
| `CullingEvent` | §3, §8 | Strict 6-field {id, runId, generationId, targetIds[], reason, scoreSnapshot}; `targetIds` = array of ids (empty array parses — count is a kernel rule §6); `reason` = non-empty string; `scoreSnapshot` = `record<string,number>` (inspectable cull justification, §8 explainability; empty `{}` parses). The persisted shape behind the `lineage.culled` event type. Frozen `packages/contracts` (P0.15 partial); field-set schema-snapshot (`spec(§8)`). |
| `LineageGraphProjection` (+ `LineageNode`, `LineageNodeType`, `LineageEdge`) | §10 | Strict 4-field {runId, nodes[], edges[], sequenceThrough}; **storage-agnostic** (no physical-storage/Neo4j field — strict). `LineageNode` = strict 6-field {id, type, label, status?, metrics?, dataRef}; `type` = closed 6 `LineageNodeType` (generation/agenome/candidate/critic/check/score); `status?` = open string (varies by node type); `metrics?` = `record<string,number>`; `dataRef` = opaque `string.min(1)` pointer (resolution is the projection-builder's job, §9, like `EvidenceRef`). `LineageEdge` = strict 5-field {id, source, target, type, label?}. `sequenceThrough` = int≥0, the per-run sequence watermark the projection was built through (§9 — rebuilt/discarded when newer events exist). Frozen `packages/contracts` (P0.13); field/node-type schema-snapshot (`spec(§10)`). |
| `FinalJudgeRubric` (+ `FinalJudgeAxis`) | §7 | **Held-out judge anchor — key safety rule #6 (immutable to agents, anti-reward-hacking).** Strict 4-field {axes, weights, policyVersion, immutableToAgents}. `FinalJudgeAxis` = closed 5 (grounding/novelty/feasibility/falsification_survival/subtype_check_pass) — no agent can add a judging axis. `axes` = `z.array(FinalJudgeAxis)` (shape-only; full-axis-set completeness is a kernel/load rule §6). `weights` = OPEN `record<string,number>` — structure frozen, values deferred-open; OPEN is REQUIRED by the §7 energy-efficiency tiebreak (a non-axis weight key). `policyVersion` = `string.min(1)` typed identically to `ScoringPolicy.version` (immutability-via-versioning, lesson §12; NO shared symbol — P0.8 YAGNI). `immutableToAgents` = `z.literal(true)` (false/omit rejected — flag unflippable). NO mutation/override/authority/scale field representable (strict + snapshot, lesson §9). **Strongest immutability pin in the package — stacks closed-axis + literal-true + required-version + no-authority.** Frozen `packages/contracts` (P0.15); field-set + `FinalJudgeAxis`(5) + literal-true schema-snapshot (`spec(§7)`). **Cross-track:** the P4/P5 held-out-judge LOAD path enforces no-agent-write + full-axis-set + `immutableToAgents:true` before scoring (Carry-forward). |
| `JudgeResult` | §7, §8 | **Held-out-judge persisted ACCEPTANCE OUTPUT (judge-output amendment).** Strict 7-field {id, candidateId, axisScores, acceptance, rubricPolicyVersion, providerMeta, langfuseTraceId?}. `axisScores`=`z.record(FinalJudgeAxis, z.number())` — the per-axis breakdown keyed by the single-source closed `FinalJudgeAxis` (lesson §5); in Zod v4 the enum-keyed record is **exhaustive + closed** (all 5 axes required, unknown axis rejected) → rule #6 defense-in-depth, the judge output can't drop/add a judging axis (⚠ Zod-v4-version-dependent — a major bump must re-run these contract tests). `acceptance`=`z.number()` — the overall metric selection consumes (surfaced as `FitnessScore.components.judge_acceptance`; the fitness↔judge link is by `candidateId` join + that component, NOT a duplicate authoritative copy — `judgeResultId` strict-rejected, mirroring the novelty link). `rubricPolicyVersion`=`string.min(1)` typed identically to `FinalJudgeRubric.policyVersion` (immutability-via-versioning, lesson §12/§17). `providerMeta`=shared `ProviderMeta` (lesson §5; rule #4 no-secret), REQUIRED. Rule #5: strict → malformed judge output rejected at persist. Rule #7: `axisScores`+`acceptance` REQUIRED so replay reads them, never re-judges (lesson §13). NO rubric/weights/immutableToAgents/scoreOverride field representable (rule #6, lesson §9). `judge.reviewed`←`JudgeResult` is the authoritative judge home (the §2.5 verifier→selection seam). Frozen `packages/contracts` (P0.16); field-set + payload-narrowing schema-snapshot (`spec(§7)`/`spec(§4)`). **Cross-track:** producer = P4.8 (verifier); consumer = P5.5 (selection). |
| `ProjectionWatermark` (+ `WatermarkedProjection<S>`) | §9 | **Demo-track-local projection contract — NOT an Appendix-A §2.5 cross-track seam.** Strict 2-field `{runId, sequenceThrough:int≥0}` — the executable form of the §9 invariant "any cached projection records the `(runId, sequence)` watermark it was built through and is discarded/rebuilt when newer events exist." `sequenceThrough` typed to match `LineageGraphProjection.sequenceThrough` (P0.13). Consumed by the `apps/api` projection builder (`buildProjection → WatermarkedProjection<S>{runId, sequenceThrough, state}`, P6.1); staleness via the pure `isStale(watermark, latestSequence)` predicate. Frozen `packages/contracts` (P6.1); field-name snapshot in `packages/contracts/test/projections/projection-watermark.test.ts` (`spec(§9)`), kept per-model NOT in the consolidated Appendix-A seam gate (it is not a §2.5 seam). |

<!-- Starts empty. The freeze-first contracts land in Phase 0 (packages/contracts); the orchestrator adds a row here as each contract model is consumed by apps/api. Canonical inventory: ARCHITECTURE.md Appendix A. -->

## Module organization

<!-- ▼ EXAMPLE BLOCK [id=module-layout]: module layout + layer dependency rule. Replace with the project's real directory tree and import-direction DAG. ▼ -->

```
apps/api/
  src/
    runtime/          # kernel: state machines, caps, energy ledger, RNG, generation loop, worker
    event-store/      # append-only writer (sequence + redaction + txn), migrations, replay reader
    model-gateway/    # ModelGateway port + provider adapters (OpenRouter, OpenAI embeddings, retrieval)
    verifier/         # critic council, held-out judge, critic rotation, injection isolation
    check-runners/    # allowlisted non-executing subtype check adapters
    selection/        # scoring, novelty, fitness, cull/parent-select, fusion, mutation
    projections/      # event-fold read models (current-state, lineage, replay summaries)
    routes/           # Fastify REST commands/queries + SSE stream + /runs/:id/health
  test/{unit,integration}/
```

Layer dependency direction (top depends on bottom, never reverse):

```
routes → projections → selection / verifier / check-runners → runtime → { event-store, model-gateway(port) } → packages/contracts
```

- Domain/runtime imports **only** `packages/contracts` + infrastructure ports — never a provider SDK, the frontend, or a projection read model.
- Provider adapters may import vendor SDKs; everything else sees only the `ModelGateway` port + `ProviderCapability`.

Cross-cutting layers can be imported from anywhere. Enforce the rule mechanically with a test where possible — the test *is* the spec for the rule (a dependency-cruiser/eslint boundary lint over §2.5 import rules).

<!-- ▲ END EXAMPLE BLOCK [id=module-layout] ▲ -->

## Subagents

See `.claude/agents/README.md` for the canonical inventory + integration points.

<!-- ▼ EXAMPLE BLOCK [id=area-subagent-candidates]: area-specific subagent candidates — list candidates that would earn their keep specifically in this area (e.g. an ABI/types syncer for a frontend area, a Pyth/feed verifier for a contracts area). Build only on real friction. ▼ -->

Candidates (build only on real friction):
- **event-schema/snapshot syncer** — when an Appendix-A model field changes, check the schema-snapshot test + the projection columns + the per-type payload map all moved together.
- **cap-invariant fuzzer** — generate over-cap spawn/energy/depth requests and assert the kernel fails closed (safety rule 1).

<!-- ▲ END EXAMPLE BLOCK [id=area-subagent-candidates] ▲ -->

## Lessons logged from prior sessions

The full prose for each lesson lives in `apps/api/LESSONS.md`. This index is the compact orientation surface.

**Lesson numbers are stable IDs** — once assigned, they don't change. New lessons get the next sequential number. `/session-end` proposes additions when it detects them; the user approves before the entry is written and a row is added here.

Lessons start at §1.

| # | Date | Topic | Rule (one-liner) |
|--:|---|---|---|
| 1 | 2026-06-20 | [strict closed contracts](LESSONS.md#1) | `z.strictObject` + `z.enum`, each pinned by a reject-out-of-set test AND a member-set/field-name snapshot · pin: `packages/contracts/test/__schema-snapshots__/field-sets.test.ts` |
| 2 | 2026-06-20 | [greenfield package toolchain](LESSONS.md#2) | workspace globs + strict `tsconfig.base` + per-package scripts + root `pnpm -r --if-present`; TS6 `paths` w/o `baseUrl` + omit `rootDir` for cross-package source (TS6059); `.prettierignore` scopes code + a doc-carrying pkg's `format:check` needs `--ignore-path ../../.prettierignore`; no per-pkg eslint config · accepted: convention (verified by /preflight) |
| 3 | 2026-06-20 | [secret-redaction scrub](LESSONS.md#3) | anchored+length-gated value-pattern + sensitive-key whole-value + secret-key de-collision; idempotent/structure-preserving/non-mutating; over-redact but keep patterns precise; env-value layer at the boundary · pin: `packages/contracts/test/security/redaction.test.ts` |
| 4 | 2026-06-20 | [contracts are pure / IO at boundary](LESSONS.md#4) | no env/file/network/clock reads in `packages/contracts`; pure fns over loaded sources, IO at the boot boundary; config deep-merge objects / replace arrays, skip JS-internal keys, field-identifying errors · pin: `packages/contracts/test/config/validate.test.ts` |
| 5 | 2026-06-20 | [shared union/helper defined once](LESSONS.md#5) | a union/type OR shared pure util (`Subtype`, `deepMerge`, `summarizeZodIssues`) needed by ≥2 in-track consumers lives in its own module + imported, never redefined; extract-and-re-point at the 2nd in-track consumer (never a 3rd mirror) — esp. load-bearing for a SAFETY helper · accepted: convention (single-source-of-truth) |
| 6 | 2026-06-20 | [schema encodes shape, not kernel rules](LESSONS.md#6) | count/range/clamp invariants (parentIds 0–2, spawnBudget clamp) are kernel-enforced, NOT in the contract — the schema stays permissive so a buggy producer is rejected by the kernel with an event, not masked · accepted: convention |
| 7 | 2026-06-20 | [discriminated-union correlation](LESSONS.md#7) | a discriminant + its dependent payload = one `z.discriminatedUnion` (variant literals from the shared union §5) so a mismatch is unrepresentable; snapshot a union via `.options`+each variant `.shape` w/ a `safeParse` discriminant probe vs the shared union's `.options` · pin: `packages/contracts/test/domain/candidate-idea.test.ts` |
| 8 | 2026-06-20 | [injection-isolation primitive](LESSONS.md#8) | trusted-instructions vs untrusted-data as distinct fields + a single-source sentinel-wrap in the frozen pkg; the wrap NEUTRALIZES embedded sentinels (output has it exactly twice for any input), single-pass-complete (marker holds a char the sentinel lacks + no self-overlap → linear DoS bound) · pin: `packages/contracts/test/verifier/critic-input.test.ts` |
| 9 | 2026-06-20 | [emit-only / no-X-field via shape](LESSONS.md#9) | pin "actor emits evidence only / no authority" structurally — `z.strictObject` of exactly the evidence fields + field-name snapshot make a winner/policy/override field unrepresentable (anti-reward-hacking); SAME technique pins rule #8 success-only accounting (EnergyEvent has no failed/retried/repaired/success field) · pin: `packages/contracts/test/verifier/critic-review.test.ts` + `test/domain/energy-event.test.ts` |
| 10 | 2026-06-20 | [all-negative test needs a positive guard](LESSONS.md#10) | a reject-only test false-passes when the export is `undefined` (`undefined.parse` throws too) — lead with a positive `Schema.parse(valid)` guard so it fails loudly if the export vanishes · accepted: convention (testing discipline) |
| 11 | 2026-06-20 | [allowlist pinned two ways](LESSONS.md#11) | rule #3 allowlist pinned (a) non-executing BY SHAPE — `z.strictObject` makes a code field (exec/command/handler/fn/script/code) unrepresentable + (b) a single-source pure gate failing safe to a schema-valid skip on unregistered id via `hasOwnProperty.call` own-property lookup (defeats `__proto__`/`constructor` bypass), fixed skip reason · pin: `packages/contracts/test/checks/check-runner-adapter.test.ts` |
| 12 | 2026-06-20 | [immutability-via-versioning](LESSONS.md#12) | rule #6 — a source carries a `version`; every artifact carries a REQUIRED identically-typed `<thing>Version` (snapshot proves present + behavioral test proves required) so the source is never mutated in place + each artifact is bound to its exact version · pin: `packages/contracts/test/scoring/fitness-score.test.ts` |
| 13 | 2026-06-20 | [authoritative-once-computed value](LESSONS.md#13) | rule #7 — an expensive-once value (embedding vector, RNG outcome, provider result) is a REQUIRED field + its provenance (embeddingModelId, dimension) so it can't be omitted + replay reads it instead of recomputing · pin: `packages/contracts/test/scoring/novelty-score.test.ts` |
| 14 | 2026-06-20 | [pinned binary for checks](LESSONS.md#14) | run format/lint/type checks via the package-pinned binary (`pnpm format:check` / `./node_modules/.bin/<tool>`), NEVER bare `npx prettier`/`eslint`/`tsc` — npx can resolve a different version + report false-clean · pattern: `npx (prettier\|eslint\|tsc)` |
| 15 | 2026-06-20 | [per-type narrowing layer](LESSONS.md#15) | narrow a generic envelope payload in a SEPARATE layer (type→schema map + own-property resolver), never by mutating the frozen envelope; fail-open to generic for unknown types, fail-closed on a known-type mismatch; snapshot the map · pin: `packages/contracts/test/events/payload-map.test.ts` |
| 16 | 2026-06-20 | [bounded payload-DoS ceiling](LESSONS.md#16) | a payload ceiling is a bounded pure primitive — depth-before-size (stringify recurses), iterative early-exit, true-byte `Buffer.byteLength`, result-object never-throws, literal-pinned constants — a security primitive the append path calls, NOT a Zod range · pin: `packages/contracts/test/events/payload-map.test.ts` |
| 17 | 2026-06-20 | [agent-immutable anchor stacks all legs](LESSONS.md#17) | a must-be-immutable-to-agents contract stacks closed-enum set + `literal(true)` flag + required identical-typed version + strict no-authority-field + value/member snapshot; completeness + no-write-path pinned at the runtime load boundary · pin: `packages/contracts/test/verifier/final-judge-rubric.test.ts` |
| 18 | 2026-06-20 | [validator returns parsed value](LESSONS.md#18) | a boundary validator returns `parsed.data` (the validated/normalized value), never the caller's input, so a present-or-future transform/coercion can't bypass onto the authoritative path · pin: `packages/contracts/test/events/payload-map.test.ts` |
| 19 | 2026-06-21 | [amending-a-freeze playbook](LESSONS.md#19) | amend a frozen contract before downstream forks as a SOLO invariant slice — author spec into docs first, then extend closed enum + bump schemaVersion + update member-set snapshot + re-record fixtures (closure + safety semantics preserved; additive = non-breaking `feat`); re-`/phase-exit` delta-scoped + re-seal · accepted: convention (process) |
| 20 | 2026-06-20 | [subsystem seam over frozen contracts](LESSONS.md#20) | a seam = a TS interface whose I/O types ARE the frozen contracts (imported, never redefined; no vendor/infra type in the surface — rule #9); conformance-test via an in-test `implements` fake + `safeParse` + a `CANONICAL_FIXTURES` registry binding (`.toBe`) so a frozen-shape drift breaks loudly; explicit-deferral wiring with first-impl/first-consumer named as real tasks · pin: `apps/api/test/unit/model-gateway/port.test.ts` |
| 21 | 2026-06-20 | [boundary env-value redaction layer](LESSONS.md#21) | env-value redaction is boundary-local (the pure frozen scrub can't host it): compose frozen `scrubSecrets` + a local pass over INJECTED secrets redacting values + array elements + **KEYS with de-collision** (keys are producer-controlled — payload is an open-key `z.record`); guard empty/short secrets (≥8 + placeholder-substring); literal `split/join`, never a built regex · pin: `apps/api/test/unit/event-store/redaction.test.ts` |
| 22 | 2026-06-20 | [verify-then-narrow a safety scrub](LESSONS.md#22) | before narrowing a safety primitive's scope, verify the reachability premise against the actual schema/code (not intuition); when you narrow, point the Step-8 adversarial reviewer at exactly that boundary; defer to verified counter-evidence — a values-only env-value scope was a [high] rule-#4 leak caught this way · accepted: convention (process) |
| 23 | 2026-06-20 | [gateway structured-output discipline](LESSONS.md#23) | validate against the request schema → accept (`parsed.data`, §18) / repair ≤1 (invalid output as `wrapUntrusted` DATA in a user message, instruction in the system message only — rule #5; reuse the FROZEN sentinel §5/§8, never a local one) / reject (caller persists `output_schema_rejected` through the P1.2 scrub); ≤1 is a structural single-`await` bound; gateway emits/persists/debits nothing · pin: `apps/api/test/unit/model-gateway/structured-output.test.ts` |
| 24 | 2026-06-21 | [fake the provider layer, not the discipline](LESSONS.md#24) | fake a seam by injecting a fake `providerCall` into the REAL `createGateway` (runs the genuine discipline — can't drift); drive valid/repairable/reject via the fake's output, never bypass the discipline; keep it stateless + deterministic (detect a repair call via the sentinel in the message, NOT a cross-call counter) + env-free (selection config resolved by the boot caller) · pin: `apps/api/test/unit/model-gateway/stub/fake-gateway.test.ts` |
| 25 | 2026-06-21 | [DB append-only = triggers + least-privilege role](LESSONS.md#25) | rule #2 append-only needs a row-level UPDATE/DELETE trigger + a statement-level TRUNCATE trigger (row-level can't catch TRUNCATE) AND a least-privilege runtime role (non-owner/non-superuser; migrations as a separate owner) — triggers are privilege-defeatable (`session_replication_role`/`DISABLE TRIGGER`). testcontainers: shared container via `globalSetup`, real PG, vitest config split (preflight Docker-free) · pin: `apps/api/test/integration/event-store/migrations.test.ts` |
| 26 | 2026-06-21 | [authoritative append path = one txn](LESSONS.md#26) | one txn: validate (`RunEventEnvelope.omit` the server/DB-assigned `sequence`+`occurredAt` — caller can't set order/clock) → `validateEventPayload` ceiling (reject `{ok:false}`, caller emits the event) → `scrubSecrets` on the PARSED payload (§18) before the only insert → advisory-lock-serialized sequence (`pg_advisory_xact_lock(hashtext(run_id))`+`COALESCE(MAX+1,0)`, closes the TOCTOU; cross-run independent) → insert; writer surface = `{append, readByRun}` only; ids parameterized; authoritative-path error messages never echo payload · pin: `apps/api/test/integration/event-store/append.test.ts` |
| 27 | 2026-06-21 | [credential boundary at config](LESSONS.md#27) | provider keys are env-only + fail-fast-checked at boot + STRUCTURALLY unrepresentable in the config object (strict schema rejects a cred field at top/nested/override layers — §9 "no-X-field-via-shape" applied to creds); `assertProviderCredentials(env)` reads injected env only (§4); errors name vars/keys never values (cf §26); merge `defaults<file<env` via the §4 discipline · pin: `apps/api/test/unit/model-gateway/registry.test.ts` |
| 28 | 2026-06-21 | [provider-adapter pattern](LESSONS.md#28) | SDK behind ONE client factory (no vendor type in the surface — rule #9; creds env-only — rule #4); reusable `retry.ts` bounded-retry + per-role-timeout (adapter config, not a `ModelRoute` field) + one-fallback, DISCRIMINATED outcome that NEVER throws on a provider failure (everything fallible — incl. fallback-route resolution — inside the envelope) + no energy accounting (rule #8); terminal `ProviderCallError` the GATEWAY maps to rejected (§6/§20 — domain never sees a throw); returns RAW output for the §23 discipline · pin: `apps/api/test/unit/model-gateway/adapters/openrouter.adapter.test.ts` |
| 29 | 2026-06-21 | [demo-safety fallback adapter](LESSONS.md#29) | a rehearsed-fallback capability (retrieval + curated corpus) FALLS BACK on terminal failure (tagged `fallbackSourced`; empty result valid) and NEVER rejects — diverges from §28's throw→reject leg — keeping the §28 invariants (bounded `withRetry`, failures surfaced, no energy on failure rule #8, vendor-free seam rule #9) + a replay-self-sufficient `EvidenceRef`-anchored-by-`eventId` shape (rule #7; mandatory non-empty eventId = kernel rule over the permissive frozen schema, lesson 6) · pin: `apps/api/test/unit/model-gateway/adapters/retrieval.adapter.test.ts` |
| 30 | 2026-06-21 | [replay-safety by construction](LESSONS.md#30) | enforce rule #7 STRUCTURALLY — a replay-path/authoritative-read component is pure with NO model/web/IO seam in scope (verify by the import list), so an external call is impossible, not runtime-guarded; fail closed on any non-Postgres pointer (external `uri` OR non-authoritative `langfuseObservationId` → `external_only`); confine unavoidable IO to the injected store + evict cached rejections · pin: `apps/api/test/unit/event-store/evidence-resolver.test.ts` |
| 31 | 2026-06-21 | [validate-not-sort + toJSON-aware canonical equivalence](LESSONS.md#31) | reading an authoritative ordered log: VALIDATE-not-sort (throw `ReplayIntegrityError` gap/out_of_order/schema_too_new — never silently re-sort/skip a corrupted log; out_of_order checked before gap) + trust the write-time boundary for shape (re-check only ordering + `schemaVersion`, no re-parse, fold `RunEventRow`); a canonical-equivalence serializer MUST mirror `JSON.stringify` `toJSON` (Date→ISO) or values collapse to `{}` → silent false-equivalence ([medium]); fold states JSON-safe + fail-loud on non-serializable · pin: `apps/api/test/unit/event-store/canonical-serialization.test.ts` |
| 32 | 2026-06-21 | [boot-config loader composition](LESSONS.md#32) | ONE `loadConfig({env,fileSources})` composes the canonical validators (`validateRunConfig`/`loadModelRegistry`/`assertProviderCredentials`) → a single deep-frozen + `readonly` `AppConfig`; `defaults<file<env` via shared `deepMerge`; credentials env-only + DISJOINT from a CLOSED explicit env→config allowlist (no `DOPPL_*` prefix sweep — a non-allowlisted secret-shaped var is un-projectable, rule #4); fail-fast `path`+`code` errors (no value echo) · pin: `apps/api/test/unit/runtime/config/loadConfig.test.ts` |
| 33 | 2026-06-21 | [state-machine transition guards](LESSONS.md#33) | a kernel state machine = a PURE `(from,to)→{allowed}\|{allowed:false,reason,from,to}` guard over a per-machine transition table (the table IS the §3 spec; full `Record<Status,…>` so a new enum member fails compile) via a shared `makeTransitionGuard` builder (§5); terminal-checked FIRST (`from_terminal`≠`illegal_transition`); decides only (no emit/mutate/IO — loop emits, appender persists); semantic/run-state preconditions stay in the kernel/loop (§6), pure transition-closure safety facts (no `spent\|failed\|culled→active` energy re-entry) in the table · pin: `apps/api/test/unit/runtime/state/generationStateMachine.test.ts` |
| 34 | 2026-06-21 | [judge-output seam + enum-keyed record](LESSONS.md#34) | a cross-subsystem ACCEPTANCE OUTPUT needs a first-class persisted model (not just a producer "persist it" bullet) — model it like its authoritative sibling (JudgeResult ~ NoveltyScore; `judge.reviewed`←JudgeResult ~ `novelty.scored`←NoveltyScore); key a closed-set breakdown with `z.record(ClosedEnum, …)` (exhaustive+closed in Zod v4 — version-dependent, gate a major bump) to derive the set from one source (lesson §5) + pin rule #6; link the downstream consumer by join + named component, never a duplicate authoritative copy · pin: `packages/contracts/test/verifier/judge-result.test.ts` _(P0.16; renumbered §32→§34 at the kernel↔cody reconciliation — collided with the kernel's boot-config §32)_ |
| 35 | 2026-06-21 | [cross-track version-counter collision](LESSONS.md#35) | a shared monotonic contract counter (`CURRENT_SCHEMA_VERSION`) bumped INDEPENDENTLY in parallel tracks off the same base COLLIDES at merge even when payload changes are disjoint (kernel v3/v4 vs cody judge v3 → 6 conflicts); resolve by UNION + version-linearize (keep all additive members, re-number by commit order — judge v3, kernel statuses fold v4); prevent by serializing cross-track bumps through the integration owner OR number-by-track + linearize at merge (extends §19's single-bumping-authority assumption) · accepted: convention (process) + pin via the kernel-020 merge `117a0ec` |
| 36 | 2026-06-21 | [cross-track merge: impl→orch doc-handoff](LESSONS.md#36) | a `git merge` conflicting in orchestrator-territory docs (`apps/api/CLAUDE.md`/`LESSONS.md`) is UNRESOLVABLE BY THE IMPL — territory-guard hook blocks impl writes there BY DESIGN (working-as-intended) — so it stalls mid-merge; bake the SEQUENCED impl→orch handoff into the cross-track-merge brief up front: impl resolves code/contract conflicts + greens + holds → hands the doc conflicts to the orch → orch resolves in-territory (UNION + renumber any collided lesson) → impl commits the single merge; no concurrent index edits in the shared worktree · accepted: convention (process) |
| 37 | 2026-06-22 | [closed-form re-derivation vs outcome bridge](LESSONS.md#37) | a per-generation deterministic choice that is a pure closed-form fn of PERSISTED inputs (run seed + a persisted index) is RE-DERIVED on replay — no outcome log, no event (the `persistOutcomes` bridge is only for draws position-dependent on the shared run stream — mutation/fusion); make the selector pure so two structural pins ride free: codomain confined to the closed target union (`CriticMandate.options`, lesson 5) can't touch the immutable judge anchor (rule #6), and no candidate/agenome param ⇒ uninfluenceable; replay-safety = lesson-30 import-list (no provider/store seam, no `Math.random`/`Date.now`) · pin: `apps/api/test/unit/verifier/council/rotation.test.ts` |
| 38 | 2026-06-21 | [app-layer isolation chokepoint](LESSONS.md#38) | rule #5 — assemble every critic/judge/check gateway request from an untrusted candidate via ONE generic chokepoint: trusted instruction in the `system` msg (candidate-independent — byte-identical regardless of candidate), candidate in a separate `user` msg wrapped by the FROZEN `wrapUntrusted` (never a local sentinel), a fixed `ISOLATION_DATA_FRAMING` const marking it data-not-instructions; generic over `ModelRole` (serves critic + final_judge, no bypass); optional schema/maxTokens omit-if-undefined; generalizes lesson §23, companion to §8 · pin: `apps/api/test/unit/verifier/isolation/candidate-as-data.test.ts` |
| 39 | 2026-06-21 | [check-runner allowlist = registry + closed impl-map](LESSONS.md#39) | rule #3 — run checks via a CLOSED descriptor-registry (frozen `resolveCheckAdapter` own-property gate, lesson §11) PLUS a PARALLEL frozen pure-impl-map keyed by the same id (the descriptor carries no code by shape → the impl-map is the new rule-#3 surface → freeze BOTH, no register API); `runCheck` resolve→`check.started`(actor check_runner, NO energy — rule #8)→run-or-skip→one validated `check.completed`; skips non-silent w/ FIXED reasons (`unregistered_adapter`/`execution_required`, never the reflected id); emit ONLY via `store.append` (forbidden #4) · pin: `apps/api/test/integration/check-runners/run-check.test.ts` |
| 40 | 2026-06-21 | [immutable-anchor load path enforces what the contract can't](LESSONS.md#40) | rule #6 — the runtime LOAD path enforces the two properties a Zod shape can't (lesson §6/§17): full-member-set COMPLETENESS (`axes.length===5` AND `Set(axes)`==the 5 members — element-enum ≠ full set; a 4-axis or 6-with-dup array parses) + an `immutableToAgents` re-assert read through an `unknown`-typed local (so `z.literal(true)` narrowing can't make it dead code); field-identifying boot error; load from a deep-frozen in-code const; source PROVENANCE (no agent-writable path) is the BOOT CALLER's documented + security-asserted obligation, not the pure fn's · pin: `apps/api/test/unit/verifier/judge/rubric.test.ts` |
| 41 | 2026-06-21 | [gateway-routed evidence producer owns its identity](LESSONS.md#41) | rule #6 — an evidence producer (council) sets its output's TRUSTED identity/correlation fields itself (deterministic id `critic-review:{runId}:{candidateId}:{mandate}` — replay), the model fills ONLY evidence via a PERMISSIVE `z.object` strip-parse (drops model-sent id/mandate/scoreOverride — barrier 1) → validate the assembled whole vs the STRICT frozen contract (barrier 2); rejection → failure event + null (never fabricated); returns evidence ONLY (no selection surface); request via the §38 isolation chokepoint, persist via `store.append`, no energy (rule #8); LLM-event meta = `langfuseTraceId` + `gatewayRequestId→correlationId` (PROVISIONAL, full ProviderMeta on P3 EnergyEvent) · pin: `apps/api/test/integration/verifier/council/run-council.test.ts` |
| 42 | 2026-06-21 | [decision aggregate computed by trusted code, not the model](LESSONS.md#42) | rule #6/#7 — the fitness-deciding aggregate (held-out judge acceptance) is computed by the RUNNER, never the model: model supplies only per-axis 0–5 inputs (two strip barriers drop a model-sent score/total/winner) → runner does the deterministic weighted sum `Σ rubric.axes of axisScores×(weights[axis]??0)` (iterate the closed axis set → non-axis `energy_efficiency` excluded, it's selection's component; no random/clock → replay-recomputable); rubric only from the immutable const (§40), candidate as DATA (§38), judge outside the loop (final_judge, not in rotation), no selection surface; emit `judge.review_started` (actor `selection_controller`) THEN persist the authoritative `judge.reviewed`←`JudgeResult` (frozen P0.16; mirrors `novelty.scored`←`NoveltyScore`) — the RUNNER builds the whole `JudgeResult` (model never supplies id/acceptance, rule #6); `acceptance` ALSO surfaces to selection P5.5 as `FitnessScore.components.judge_acceptance` by candidateId JOIN, not a duplicate. **CORRECTED by verifier-010** — the original "no judge.reviewed / RETURN for selection" claim was a pre-P0.16 fork-divergence artifact · pin: `apps/api/test/unit/verifier/judge/judge-call.test.ts` |
| 43 | 2026-06-21 | [subtype check adapter = pure CheckRunner over the frozen payload](LESSONS.md#43) | rule #3 — a subtype check adapter is a PURE `CheckRunner` (`(input)=>CheckResult`, no IO) that parses the candidate as its frozen subtype payload (DATA, fail-not-throw via a shared wrapped-parse helper → `null`→`failed`) and checks RELATIONS not presence (the `strictObject` already guarantees presence — relations like cross-domain/token-overlap/word-count are where the deterministic signal is), thresholds as named tunable consts; the closest-to-executable adapter carries an explicit never-executes-candidate-code test. Retrieval-grounded checks DON'T fit a pure-sync harness (the curated-corpus FAKE is available — blocker is the harness SHAPE, not retrieval) → defer to an async-harness-or-caller-does-retrieval slice + flag so the shipped set is honestly N-of-M · pin: `apps/api/test/unit/check-runners/transfer/source-validity.test.ts` |
| 44 | 2026-06-21 | [retrieval-grounded check stays pure via caller/adapter split](LESSONS.md#44) | rule #3/#7 — keep a retrieval-grounded check a PURE adapter by SPLITTING retrieval from scoring: the CALLER (P3) fetches the gateway `retrieval` source once + persists the outcome (§9 replay home) + threads results into the pure adapter as DATA (re-threads the persisted outcome on replay); the adapter scores candidate-vs-results deterministically, makes NO provider call (rule #3 non-executing + rule #7 replay-safe BY CONSTRUCTION), `skipped{retrieval_unavailable}` on absent results (never `failed`, never re-fetch), and RECORDS its grounding (`evidenceRefs`, EvidenceKind prior_art/signal) — but the FULL-results persistence is the caller's, not the adapter's. Sub-lesson ([high] fixed in-slice): GUARD caller-supplied DATA before it crosses into a strict contract field (`source:''`→`EvidenceRef.label.min(1)` throws → breaks fail-not-throw) · pin: `apps/api/test/unit/check-runners/transfer/prior-art.test.ts` |
| 45 | 2026-06-21 | [live-rerun-with-replay-fallback](LESSONS.md#45) | rule #3/#7 — try-live through the SAME gated runner verbatim (`runCheck` — no new exec surface; unregistered id → skipped via the frozen gate); on throw/skip serve the most-recent recorded USABLE (passed/failed) result READ-ONLY from the authoritative log (`readByRun` — no append/provider/re-sample, rule #7), re-parsed (`CheckResult.safeParse`, lesson §20); never fabricate (fixed-reason skip when no usable verdict). LOAD-BEARING status filter: the live `runCheck` appends its OWN skip (highest seq) BEFORE the fallback reads (append-then-return), so filter to passed/failed or it serves the triggering skip back — a read following a write in the same flow sees that write · pin: `apps/api/test/unit/check-runners/live-rerun.test.ts` |
| 46 | 2026-06-22 | [type-blind redaction corrupts non-string sensitive-key fields](LESSONS.md#46) | rule #4/#7 — a type-blind whole-value scrub (sensitive-key → placeholder regardless of type) corrupts a non-string field whose key merely CONTAINS a sensitive substring (`tokensIn`⊃`token` → the integer redacted to the STRING `[REDACTED]` → the persisted payload fails its frozen contract's `safeParse` on read = SILENT authoritative-log corruption, rule #7; the append succeeds because the payload-map narrows the PRE-scrub payload); fix is type-aware NOT scope-narrowing — redact a sensitive-key value UNLESS it is number/boolean (a credential is always a string), keep whole-redaction for strings AND objects/arrays; the naive typeof-string guard LEAKS object-valued secrets — let the existing structured-value + no-secret-corpus tests adjudicate the fix shape · pin: `packages/contracts/test/security/redaction.test.ts` |
| 47 | 2026-06-21 | [seeded-RNG replay-determinism by construction](LESSONS.md#47) | persist the RNG OUTCOME (the index/number a draw resolved to) not the derived value (a non-JSON-safe result can't silently diverge on replay); keep the replay outcome source PRNG-free (imports no seeded RNG — re-sampling structurally impossible, not flag-guarded, extends §30; LIVE depends on a local `RngDraws` interface `SeededRng` satisfies); read the ordered outcome log VALIDATE-not-sort (throw on label-mismatch/overdraw/index-out-of-range, §31); never expose an un-recorded `rng.pick` that bypasses the log (rule #7 hazard — dropped, no consumer); PRNG raw surface = `nextUint32`/`nextFloat`/`nextInt` only · pin: `apps/api/test/unit/runtime/rng/persistOutcomes.test.ts` |
| 48 | 2026-06-21 | [caps + kill switch = pure decisions](LESSONS.md#48) | kernel caps + kill switch are PURE decisions (loop emits/halts/drains, extends §33): caps = fail-closed `(consumed,requested,RunConfig.caps)→decision` with NO trait/spawnBudget param (a trait can't raise a cap by shape, rule #1) + total across all 6 dims (inclusive count ceiling, exclusive injected-clock deadline, cf §37); kill switch maps each non-terminal to its §3-LEGAL terminal via the P3.2 guards (per-state — configured→cancelled / running→stopping\|failed / pending→skipped / active→failed — NOT a literal universal →failed) + EXCLUDES already-terminalizing/transient states (completing/stopping/degraded drain via their in-flight step under a latching halt); configured→cancelled + pending→skipped are status-only (no registry event → terminalEvent:null, rule-#2 gap escalated) · pin: `apps/api/test/unit/runtime/caps/{capEnforcer,killSwitch}.test.ts` |
| 49 | 2026-06-21 | [success-only energy ledger](LESSONS.md#49) | energy ledger = a PURE success-only debit (loop emits, extends §33): success-only is STRUCTURAL (frozen `EnergyEventType` has no failure member → a failed attempt can't yield an EnergyEvent, rule #8 by shape, lesson §9); llm `actual` derives ONLY from real ProviderMeta (PM required by the eventType-discriminated reconcile input — compile error to omit + fail-loud backstop §31 — never falls back to estimate); `cumulativeSpend` folds ACTUAL not estimate per scope (cap enforcer sees true spend), trusting write-time validation/no-re-parse (§31); cost config-driven (`DEFAULT_COST_MAP` §4, `ceil` rounding — never under-debit); emission + secret-scrub deferred to the loop (P3.10 git-merge-cody scrub fix, L21 trap) · pin: `apps/api/test/unit/runtime/energy/{estimateReconcile,energyLedger}.test.ts` |
| 50 | 2026-06-21 | [run full /preflight (incl format:check) every slice](LESSONS.md#50) | a per-slice SUBSET check (lint+type+tests, no `format:check`) lets prettier drift land in committed files — caught later by the repo-wide `format:check`, forcing a retro chore (hit P3.4 `dac730d` + P3.5 `bf99d59`, fixed at kernel-025); run the FULL `/preflight` (sync→lint→format-check→type→test) OR at least `format:check` every `/tdd` Step 7; keep any retro fix a SEPARATE whitespace-only `chore(format)` commit (never bundled into a feat); same family as §14 · accepted: process (run /preflight per slice; pre-commit format:check hook is the mechanical backstop if it recurs) |
| 51 | 2026-06-21 | [projection = pure ordered fold + watermark](LESSONS.md#51) | a projection is a generic reducer-injected PURE fold over `(runId, sequence)` → a watermark-tagged byte-stable result (exported `canonicalize` = sorted-key JSON, reused by P6.4); ASSERT strict consecutive monotonic ordering + reject `schemaVersion>current` + reject empty/mixed-run as a typed closed-reason `ProjectionError` (never silently fold/re-sort/skip); rebuild is provider-free (rule #7, no-import test §10); staleness = pure `isStale(watermark, latestSequence)` + thin parameterized `latestSequence(db, runId)` (runId opaque) · pin: `apps/api/test/unit/projections/projection-builder.test.ts` |
| 52 | 2026-06-21 | [a second persistence boundary mirrors the first exactly](LESSONS.md#52) | rule #4 second boundary (observability before Langfuse emit) mirrors the event-store twin's FULL discipline — same `enforcePayloadCeiling`-then-scrub ORDER (not just the scrub — a missing ceiling = unguarded recursion / stack-blow), same boundary-local env-value layer (keys+arrays+values, de-collision, ≥8 guard, literal split/join, proto-safe rebuild) composed over frozen `scrubSecrets` (never hoist/reimplement); inject the boundary IO (`createEmitBoundary`, L24) + fail safe (failed export → local warn, NO authoritative-log write, §13, no-DB-import test); cross-track consumer (P2.8) imports the canonical scrub, never duplicates · pin: `packages/observability/test/{redaction,emit}.test.ts` |
| 53 | 2026-06-21 | [concrete projection = reducer injected into the fold](LESSONS.md#53) | build a concrete projection as an immutable reducer INJECTED into the §51 `buildProjection` (never hand-roll the fold); rows keyed-by-id + SET (idempotent re-fold); high-traffic rows = frozen payload verbatim + read persisted values without recompute (rule #7); the event→entity-transition map is grounded in the FROZEN status enums (a marker is durable status only if the enum has the value — `generation.verifying/scoring/reproducing`→GenerationStatus; the 8 op-markers no-op for the live view §12); `energy_exhausted` mid-flight not terminal (§5); cull→status, reproduction→edges; an un-evented transition is DEFERRED as an integration-reconcile carry-forward, never guessed from side-effects · pin: `apps/api/test/unit/projections/current-state.test.ts` |
| 54 | 2026-06-21 | [secondary projection = pure transform; render-graph drops dangling edges](LESSONS.md#54) | a projection another already covers is a PURE TRANSFORM of it (carry the watermark through, never re-fold); as a producer of a frozen Appendix-A model don't touch the contract — pin producer-conformance (output `safeParse`s the frozen schema = the §2.5 check, no new snapshot); a render-bound graph's edge set must CONNECT the tree (genealogy lineage_edges + structural derivation edges over the open `LineageEdge.type`) and DROP an edge with a missing endpoint (React Flow breaks on dangling) + use UNIQUE edge ids (kind-prefix `struct:`/`repro:` so a shared source→target never collides — RF ALSO breaks on DUPLICATE edge ids, `demo-020` gate-fix); encode non-node concepts as status/metric on the CLOSED node-type set (winner = candidate status 'selected', novelty = metric); `dataRef` = within-tier id · pin: `apps/api/test/unit/projections/lineage-graph.test.ts` (incl. `test_edge_ids_unique`) |
| 55 | 2026-06-21 | [replay path = rule-#7 surface, structurally pinned](LESSONS.md#55) | enforce rule #7 at the replay path STRUCTURALLY: give the reader a narrowed type (`Pick<EventStore,'readByRun'>`) so append/writes are unreachable (rule #2+#7); pin no-provider two ways — an import-ban test (transitive path = only `@doppl/contracts` + relative modules + runtime-erased `import type`) AND a call-shape test (no `fetch(`/`Math.random(`/provider symbol); re-fold the persisted log via the §51/§53 builders (schemaVersion-≤-current gate ON the path, reused not bypassed) reading RNG/embedding/retrieval from persisted payloads verbatim (never re-sample/re-embed/re-call); state-equivalence = `canonicalize(replay)===canonicalize(captured)` (L51, a determinism guard); commit an older-`schemaVersion`(v1) fixture that replays · pin: `apps/api/test/unit/projections/replay-summary.test.ts` |
| 56 | 2026-06-21 | [REST write path: sanitizing boundary + ingestion gates](LESSONS.md#56) | Fastify write path = `bodyLimit` ingestion gate (before the P0.10 ceiling) + a `setErrorHandler` that SANITIZES 5xx to `{error:'internal_error'}` (no internal-message leak at the trust boundary; 4xx pass through; server-stdout `request.log.error` is OUTSIDE the rule-#4 event-log/Langfuse/UI boundary); `validateRunConfig` at ingestion (reject a non-object body, no event on invalid); cap overrides rejected above the validated maxima as an API DEFENSE layer (kernel = authoritative enforcer, rule #1); idempotency-key dedup + one-active-run via an in-memory hint RE-VALIDATED against the authoritative log (in-memory = §5 single-process MVP; persisted dedup + log-wide scan = hosted/P3 hardening); REST appends authoritative events ONLY, never mutates a projection (rule #2, no `.insert`/drizzle in the route) · pin: `apps/api/test/integration/routes/runs.test.ts` |
| 57 | 2026-06-21 | [REST read surface: rebuild-on-read + clean 404 + cross-track read-import](LESSONS.md#57) | the GET read surface registers on the shared Fastify server + REBUILDS each projection from `readByRun` on read (MVP — always fresh; dashboard_snapshots cache + watermark-staleness deferred); unknown id → clean 404 (never partial/empty 200); reads are read-only (no append/projection-write — rule #2); when a read needs another TRACK's area data, keep the reader IN-AREA + READ-IMPORT that area's schema (`listRunIds` in `projections/` read-imports `event-store/schema`, `selectDistinct` — zero kernel-file edits; a read-only cross-area import is fine, editing isn't) · pin: `apps/api/test/integration/routes/runs-read.test.ts` |
| 58 | 2026-06-21 | [run-health = read-only log-derived signal; count-based ops-in-flight; clamped caps](LESSONS.md#58) | `GET /runs/:id/health` is a read-only rebuild-on-read projection of the log (no provider, distinct from Langfuse): operations-in-flight = `max(0, count(*_started) − count(completion))` per op family (exclude durable generation phase-markers; judge pairing awaits sv3 `judge.reviewed`; a failed op stays counted — MVP, matches literal "unpaired" spec); caps-consumed CLAMPED `min(consumed, ceiling)` per cap (never over-report, null if no caps); candidates-in-flight = non-terminal status; lastEventAt = highest-sequence occurredAt; unknown id → 404; shape apps/api-internal (promote at P7.14) · pin: `apps/api/test/integration/routes/run-health.test.ts` |
| 59 | 2026-06-21 | [SSE run-event stream = delivery-only bridge polling readByRun; id=sequence resume](LESSONS.md#59) | `GET /runs/:id/stream` is a delivery-only, non-authoritative live feed (rule #2): a demo-owned async-generator bridge POLLS `readByRun` past the cursor (read-imports the event store — NO kernel-file edit, like P6.7 `listRunIds`; append→notify bus = deferred hosted/P3 optimization); SSE `id`=event `sequence` so `Last-Event-ID` (header + `?lastEventId` fallback, numeric-guard → 400 if present-but-invalid; absent → from 0) resumes gap/dup-free (`sequence>cursor` only); carries op-start markers AND completions (§4/§12 live window); pinned rule #2 two ways (event-count-unchanged + re-stream byte-identical) + drop+resync === uninterrupted (sequence sole ordering) + fallback `GET /events` reconstructs identical ids; unknown id → clean 404; `reply.hijack()` + raw `text/event-stream` single-line `data` framing (Fastify v5, no frame-injection), client-disconnect → `AbortController.abort()`; injected `sleep`/`maxIdlePolls=1` keeps tests timer-free (`buildServer` gains `sse?: EventBridgeOptions`, prod default real sleep/∞) · pin: `apps/api/test/integration/routes/run-stream.test.ts` + `test/unit/sse/event-bridge.test.ts` |
| 60 | 2026-06-21 | [runtime self-observability built ahead of the worker; logger stamps §4 correlation IDs (console local-unscrubbed, external reuses §52); heartbeat = injected-clock throttle + pure staleness](LESSONS.md#60) | two observability primitives built AHEAD of the live worker (P3 absent on the demo fork, like P6.1/P6.9) + injected-everything: the kernel-logger stamps the §4 envelope correlation IDs (runId req + generationId?/agenomeId?/correlationId?) to an injected sink (default console) — the local `log()`/console path is NOT scrubbed (process trust boundary, LESSONS §56; secrets never reach the input by the env-only guarantee), the ONLY external path `emitExternal` REUSES the §52 `createEmitBoundary` (ceiling-then-scrub, rule #4, never reimplemented; no boundary = no-op); the heartbeat is an injected-clock throttle (≤1 beat/intervalMs, no real setInterval/Date.now) + pure `isWorkerAlive(lastBeatAt, now, staleAfterMs)` (null-never-beat + stale-window → not-alive); BOTH modules structurally barred from the append path (import-ban test — never block/mutate the log) + emit NO `run_events` (a heartbeat/log is a side signal, not in the closed 36-type registry); console + injected sink only, no external metrics stack (§13 MVP, import-ban over datadog/prom-client/prometheus/statsd/@opentelemetry); worker-loop + `/health` last-heartbeat wiring DEFER to P3/integration · pin: `packages/observability/test/kernel-logger.test.ts` + `apps/api/test/unit/runtime/heartbeat.test.ts` |
| 61 | 2026-06-21 | [run format:check in the per-slice gate, not just /preflight](LESSONS.md#61) | the per-slice `/tdd` gate (`lint && typecheck && test`) does NOT run `pnpm format:check` — only `/preflight` + `/phase-exit` do — so a slice lands format-dirty + prettier drift ACCUMULATES silently across committed slices until the phase boundary trips on the pile-up (P6.9/P6.10 test files caught at the P6.11 boundary; same class as LESSONS §14 npx-false-clean); add `pnpm format:check`/`prettier --write` (package-pinned, never bare npx — §14) to the PER-SLICE gate; fix accumulated drift as a standalone format-only `style:` commit separate from the `feat:`; durable fix = fold format:check into the /tdd Step-8 gate (scaffold change → lead) · accepted: convention (process); durable fix = /tdd-gate scaffold edit |
| 62 | 2026-06-22 | [sv-skew reconcile is additive when downstream consumes-not-emits](LESSONS.md#62) | an sv-skew projection reconcile (downstream CONSUMES the advanced contract, EMITS none of the new events) is ADDITIVE + decomposes by sv-delta KIND: a new event type carrying a frozen entity → keyed VERBATIM row (rule #7, mirror novelty/fitness rows); a new terminal event type → type→status transition to the frozen terminal (mirror candidate_invalidated→invalid); a new STATUS value with NO event type (kernel-INTERNAL state-machine status — GenerationStatus degraded, CandidateStatus repairing) → NO reducer transition (the fold has no signal to key on; a guessed transition invents a log signal that never fires). Distinguish "the frozen enum HAS the value" (display-layer status-map exhaustiveness) from "an event CARRIES the value" (reducer fold branch) — only the latter warrants a transition; pin the no-transition decision with a test exhaustive over the closed event registry · pin: `apps/api/test/unit/projections/current-state.test.ts` (test_degraded_repairing_have_no_event_transition) |
| 63 | 2026-06-21 | [audit ALL terminals vs the registry, not one gap at a time](LESSONS.md#63) | a closed recording-event registry must cover EVERY reachable §3 state-machine terminal (rule #2 — each terminal persistable/replayable) or carry a rule-#2-valid projection (selected=scored∧¬culled, culled=lineage.culled, invalid=candidate_invalidated, reproduced=agenome.reproduced); when a gap surfaces (kernel-026: killSwitch `terminalEvent:null`), sweep ALL machines' terminals HOLISTICALLY not one-at-a-time — the sweep found 4 missing at once (run.cancelled/generation.skipped/agenome.failed/candidate.rejected → sv5); corollaries: a registry-add MAY precede its emitter (defined-but-not-yet-emitted is rule-#2-valid) + separate true terminals from intermediate states (AgenomeStatus spent/eligible_parent are intermediate, no event) so the audit doesn't over-count (extends §19/§35/§36) · pin: `packages/contracts/test/events/event-type.test.ts` (41-member + closure) · accepted: convention (process) |
| 64 | 2026-06-22 | [loop = pure orchestration, never authors seam events](LESSONS.md#64) | model a kernel↔subsystem loop as pure orchestration over INJECTED seam ports — the loop appends ONLY kernel-owned events (via the append path) + consumes seam-owned events (critic.reviewed/fitness.scored/lineage.culled/…) as DATA, NEVER authoring them (§2.5 boundary as code shape, rule #9); build + test vs FAKED seams against the frozen contracts (selection P5 unmerged, verifier retired) so real wiring is drop-in; techniques: the no-op-seam test (assert ZERO seam-owned types authored) + an observability signal the frozen contract can't carry rides a runtime-local port COMPOSING the frozen one (tool_call relay via GenerationGateway, not a contract edit) · pin: `apps/api/test/unit/runtime/loop/generationLoop.test.ts` · accepted: convention (process/architecture) |
| 65 | 2026-06-22 | [scoped cross-track merge leaves mirror docs stale vs contracts](LESSONS.md#65) | a SCOPED forward-merge (only the contract commit, not the doc-routing commit) updates the integration branch's CONTRACTS but not its orchestrator-territory mirror docs (cody had sv5 contracts but stale RunEventType 37/sv4 CLAUDE.md cross-doc rows) → on the back-merge DON'T wholesale `git checkout cody -- CLAUDE.md` (re-introduces stale rows); reconcile PER-FIELD — keep the more-current cross-doc rows (track's sv5, auto-kept) + adopt the integration's canonical lessons index (§1–62) + renumber track-local above it (§41→§63/§42→§64, drop §37–40 dups ≡ cody §47–50) · extends §35/§36 · accepted: convention (process) |
| 66 | 2026-06-22 | [redaction test fixture trips gitleaks on the var name — guard-clean, never --no-verify](LESSONS.md#66) | a redaction test that plants a fake secret trips the gitleaks pre-commit guard on the VARIABLE NAME (`const SECRET=…` → generic-api-key rule), not the value entropy → make the fixture guard-clean (low-entropy value + keyword-free var name; the env-value redaction is name-agnostic so intent is unchanged), NEVER `--no-verify` past the secrets-guard (security-posture violation even for a known-fake fixture; the guard is the rule-#4 mechanical backstop) · pin: `apps/api/test/unit/runtime/loop/generationLoop.test.ts` · accepted: convention (test hygiene + security) |
| 67 | 2026-06-22 | [run-terminal classification = pure log decision + thin executor](LESSONS.md#67) | the run's terminal verdict is a PURE `classifyRunTerminal(log,{killSummary?,crashed?})` over the persisted scored-survivor projection (`fitness.scored ∧ ¬lineage.culled`, the `resolveEligibleParents` set §54/§63) + a thin append-path executor at the caller (mirrors planKillSwitch/executeKillAndDrain); guard-validates the full §3-legal path from the ACTUAL current status via pure `runTerminalPath(from,terminal)→RunStatus[]\|null` (transient completing/stopping routed, no event §62; already-terminal→null closes double-emit, rule #2); replay-stable by construction (import-ban + determinism, §30/§55) · pin: `apps/api/test/unit/runtime/terminal/terminalClassifier.test.ts` · accepted: convention (architecture) |
| 68 | 2026-06-22 | [MVP selected best-so-far = top scored survivor; never blocks on absent P5](LESSONS.md#68) | §3 ends a run completed iff a `selected` best-so-far exists, but selection (P5) + `candidate.selected` aren't in-track → derive a replay-stable MVP stand-in from kernel-persisted signals: finalIdeaRef = top-`total` `fitness.scored ∧ ¬culled` survivor, tie-break LOWEST sequence (never occurredAt/insertion order); P5's authoritative `candidate.selected` supersedes by candidateId join, never a duplicate — the kernel produces a usable final idea NOW rather than blocking · pin: `apps/api/test/unit/runtime/terminal/terminalClassifier.test.ts` · accepted: convention |
| 69 | 2026-06-22 | [energy_exhausted is MID-FLIGHT, not a run-terminal](LESSONS.md#69) | `energy_exhausted` looks terminal (energyBudget kill emits it for running→failed) but §5 / reducer `lifecycle.ts:11-13` define it as a run-status NO-OP (drain + score-already-verified, then the REAL terminal follows) → the classifier's already-terminal no-op set is `{run.completed/failed/stopped/cancelled}` ONLY (exclude it, else the run is stranded non-terminal forever); after it the survivor decision still runs (pre-exhaustion survivor ⇒ run.completed, else run.failed) — the energy killSummary FALLS THROUGH, never short-circuits to run.failed · pin: `apps/api/test/unit/runtime/terminal/terminalClassifier.test.ts` · accepted: convention (rule #2 + §5) |
| 70 | 2026-06-22 | [worker = loop's production caller + run.started owner; concurrency/idempotency from the authoritative log](LESSONS.md#70) | `runWorker` is `runGenerationLoop`'s production caller + owns `run.started` (configured→running, guard-validated; only for a `configured` run — run-level idempotency via `run.configured`∧¬`run.started`); single-active-run = pure `activeRunGuard` over `isRunTerminal(log)` (terminal set from `RUN_TERMINALS`, never a fold mirroring projections/lifecycle.ts; energy_exhausted ∉ set ⇒ active); idempotency keys off the persisted sequence watermark not the loop's in-memory eventSeq; runtime can't import projections / edit the EventStore port → run enumeration is an INJECTED `listRunIds` (reuses projections §57 via DI); the REST activeRunId hint mirrors, the kernel guard decides · pin: `apps/api/test/{unit/runtime/worker/*,integration/runtime/run-worker}.test.ts` · accepted: convention (architecture) |
| 71 | 2026-06-22 | [per-iteration liveness side-signal rides an additive onIteration hook, never a side-effecting clock](LESSONS.md#71) | a heartbeat that must fire "each loop iteration" rides an explicit additive `onIteration?:()=>void` on `GenerationLoopDeps` (default no-op → zero behavior change, existing loop tests green; worker passes `onIteration:()=>heartbeat.beat()`), NOT a side-effecting clock (`now=()=>{beat();return clock()}` — conflates timekeeping w/ liveness via a double clock() read + couples beat cadence to now()-call-sites so removing one silently stops beats); stays a §60 side signal (no run_event, rule #2), injected clock+sink+operatorStop for timer-free determinism (`intervalMs:0` in test) · pin: `apps/api/test/unit/runtime/worker/runWorker.test.ts` · accepted: convention (rule #2 + §60) |
| 72 | 2026-06-22 | [crash-forward maps each orphaned run to its §3-legal crash terminal per log-status](LESSONS.md#72) | §5's "mark any non-terminal run failed" does NOT map 1:1 onto §3 (like §48 for the kill switch): `run.failed` needs `from=running`; a never-started `configured` run can only go `configured→cancelled` (`configured→failed` illegal → `runTerminalPath('configured','failed')`=null) → crash-forward maps per log-observable status: `running→run.failed{crash}` (via `classifyRunTerminal(crashed:true)`), `configured→run.cancelled{crash}`; completing/stopping are transient (Q4, no event) so don't arise; a crashed running run with a pre-crash survivor is forward-FAILED (survivor in the partial summary), not finalized — §5 never-resumes; layers on the P3.11 classifier (terminalClassifier.ts untouched) · pin: `apps/api/test/unit/runtime/recovery/crashForward.test.ts` · accepted: convention (extends §48) |
| 73 | 2026-06-22 | [crash-forward = the worker's clean-slate precondition](LESSONS.md#73) | P3.12's guard treats any non-terminal run as active (`isRunTerminal` false) → crash-forward runs BEFORE the worker accepts work + terminalizes EVERY non-terminal run incl. a never-started `configured`→cancelled (leaving it would make the guard see it active + block all new runs); deterministic over the log (PURE, import-ban), idempotent (re-run appends nothing — terminal it wrote → skipped), append-only via P3.3 guard-validated; boot-sequence caller (crash-forward→then worker) is Phase-D bootstrap territory (deferred) · pin: `apps/api/test/integration/runtime/crash-forward.test.ts` · accepted: convention (§5 + P3.12) |
| 74 | 2026-06-22 | [seam adapter composes runners via per-gen emit shim](LESSONS.md#74) | a kernel↔subsystem seam adapter MATCHES the injected port (never edits it — a mismatch is a cross-track Finding) + COMPOSES the subsystem's existing runners behind it (no rebuild): writes route through a per-generation `{append: ctx.append, readByRun: deps.read}` shim (never a deps-closure write, rule #2; sub-runner signatures untouched), a per-gen derivation the port omits is read from the authoritative `*.started{index}` event (never the id string — IDs opaque), run-level constants close over config; STRICT subtype filter excludes subtype-less placeholders (no authoritative-log pollution); all safety INHERITED from the composed runners (no new safety logic); production wiring is the consumer's boot root (cross-track), proven in-track by an integration test driving the REAL loop · pin: `apps/api/test/integration/verifier/verify-seam.test.ts` · accepted: convention (extends §64/§20) |
| 75 | 2026-06-21 | [replay-split for stochastic+provider ops](LESSONS.md#75) | live persists every outcome; apply* reconstructs zero-rng/zero-gateway (structural, no gateway param) · pin: `test/unit/selection/reproduction/{mutate,fuse,reproduce}.test.ts` |
| 76 | 2026-06-21 | [fail-loud replay-integrity](LESSONS.md#76) | typed guards throw on a corrupt persisted event; never String()-coerce a tampered log · pin: `test/unit/selection/reproduction/fuse.test.ts` |
| 77 | 2026-06-21 | [selection-decides / kernel-emits-lifecycle](LESSONS.md#77) | selection emits domain events + flags; kernel owns lifecycle/state/energy/seed/gen-minting · accepted: convention (architecture) |
| 78 | 2026-06-21 | [numeric-only fitness-component purity](LESSONS.md#78) | components read numeric scores only, never candidate/critic text (rule #5/#6) · pin: `test/unit/selection/components/{critic-scores,judge-acceptance}.test.ts` |
| 79 | 2026-06-21 | [consume-an-immutable-anchor + single-source](LESSONS.md#79) | read judge acceptance verbatim; load-gate full-5-axis+immutable+version; one rubric → verify+score for the join (rule #6) · pin: `test/unit/selection/components/judge-acceptance.test.ts` + `test/integration/boot/compose-runtime.test.ts` |
| 80 | 2026-06-21 | [clamp-as-hint: selection proposes, kernel bounds](LESSONS.md#80) | allocation/threading/per-run-config clamp to min(_, remaining caps); never raises (rule #1) · pin: `test/unit/selection/allocation.test.ts` + `test/unit/runtime/loop/generationLoop.test.ts` |
| 81 | 2026-06-21 | [shared SelectionEmitter](LESSONS.md#81) | one emitter type (= EventStore.append minus server fields) imported, never redefined · accepted: convention (single-source-of-truth) |
| 82 | 2026-06-22 | [seam-impl-factory wiring](LESSONS.md#82) | create<Seam>(deps) composes unit-pinned fns + reads inputs via readByRun + emits only via ctx.append; anchors injected from boot · pin: `test/integration/selection/{score-seam,reproduce-seam,successor-threading}.test.ts` |
| 83 | 2026-06-22 | [jsonb-key-reorder breaks order-sensitive provenance](LESSONS.md#83) | canonicalize (sort keys) before Object.keys/JSON.stringify provenance; jsonb drops key order → replay divergence; pin with a key-reordered + real-PG round-trip test (rule #7) · pin: `test/unit/selection/reproduction/mutate.test.ts` + `test/integration/selection/reproduce-seam.test.ts` |
| 84 | 2026-06-22 | [boot root = single IO + composition site](LESSONS.md#84) | all process IO (env/db/listen) at main.ts; seams pure/injected; boot order loadConfig(fail-fast env)→migrate→AWAIT crashForward→listen (worker never accepts work pre-recovery, §73); one listRunIds(db) into crashForward+worker; defaultConfig.caps==boot ceiling (rule #1); testable bootApp behind a guarded entry runner; real-PG fresh-DB-per-test · pin: `test/integration/boot/main-boot.test.ts` |
| 85 | 2026-06-22 | [operator-abort = latching signal, worker terminalizes](LESSONS.md#85) | stop SIGNALS a latching in-memory channel the worker polls (operatorStop), never an in-route terminal append (can't stop a live worker + double-terminal risk); route returns 202, worker drains run.stopped (running→stopping, actor runtime, rule #2); channel created once at main.ts, threaded via optional fields, cleared on settle; pin prod wiring by booting the drain test via bootApp · pin: `test/integration/boot/operator-stop.test.ts` |
| 86 | 2026-06-22 | [replay-fixture dump = validate-through-replayEvents + path-guarded read-only writer](LESSONS.md#86) | dump THROUGH replayEvents (fail loud on corrupt, never resort); isRunTerminal gate (failed/cancelled dumpable, energy_exhausted excluded); pin top-level schemaVersion=max(rows); read-only + import-ban the PROVIDER seam (not DB/fs); secretValues:[] (scrub ran at append); assertSafeRunId guards an argv-derived path; pure builder + IO behind a guarded runner · pin: `test/{unit,integration}/event-store/dump-replay.test.ts` |
| 87 | 2026-06-22 | [replay-fixture seed = direct restore-insert under the INSERT-allowed trigger](LESSONS.md#87) | DIRECT insert preserving recorded (sequence,occurredAt) (append path re-allocates, can't restore); idempotent onConflictDoNothing on (run_id,seq) — legal because the append-only trigger allows INSERT; gate schemaVersion≤current (re-record not upcast) + replayEvents re-validate + to_regclass after-migrations probe + shared path-guard + committed-only; per-event RunEventEnvelope(null-stripped copy)+validateEventPayload at the seed boundary (restore bypasses append-path validation, §46 CLOSED in PD.3-completion) · pin: `test/{unit,integration}/event-store/seed-demo.test.ts` |
| 88 | 2026-06-22 | [env-gated conditional boot-seed + pool-cleanup on abort](LESSONS.md#88) | boot = loadConfig→migrate→SEED(env-gated DOPPL_SEED_FIXTURE; absent→no-op live boot)→crashForward→listen; seed after db, before crashForward (terminal seeded run is crashForward-inert); missing/invalid fixture aborts BEFORE listen (never serve half-seeded, §17); bootApp ends the pg pool on ANY abort path (try/catch+rethrow — fixes a latent boot-spine leak) · pin: `test/integration/boot/main-boot.test.ts` |
| 89 | 2026-06-22 | [demo cap-override shares the authoritative boundary + defers to it](LESSONS.md#89) | a demo/operator convenience that narrows a safety bound (cap-lowering override) shares the AUTHORITATIVE check's exact boundary (`> maxima` rejected / `≤` allowed, parity with route overCapField), DEFERS to the kernel/route as sole enforcer (rule #1 — never a 2nd authority), re-validates output via RunCaps.parse (closes NaN/float/coercion gaps), + is backed by an integration test that the authoritative layer still 422s + appends ZERO run.configured (rule #2) · pin: `test/integration/runtime/demo/cap-override-write-path.test.ts` |
| 90 | 2026-06-22 | [live gateway = real adapter into the SAME createGateway; selector honest-throws](LESSONS.md#90) | createLiveGateway = createOpenRouterProviderCall fed into the SAME createGateway (validate/repair/reject + ProviderCallError→rejected + no-energy INHERITED, never re-implemented); selectGateway(selection, liveDeps?) = thin recorded-vs-live multiplexer — HONEST-THROW on absent live deps, NEVER a silent fake fallback (a DOPPL_GATEWAY=live misconfig must fail loud, not run secretly recorded); main.ts builds live deps LAZILY (only when live; recorded builds no client); key env-only (rule #4) + SDK behind the OpenRouterClient seam (rule #9); live boot tested via injected fake client · pin: `test/unit/model-gateway/{live-gateway,select-gateway}.test.ts` |
| 91 | 2026-06-22 | [per-run problem into generation = wrapUntrusted DATA + output validated via derived content schema](LESSONS.md#91) | INPUT (rule #5): the per-run problem (RunConfig.seed, was dropped in mergePerRunConfig) reaches generation ONLY as wrapUntrusted DATA — population_generator req = messages {system=agenome.systemPrompt+fixed framing (trusted) / user=wrapUntrusted(problem) (untrusted)} via the contracts wrapUntrusted primitive (LESSON 38), NOT the verifier assembler (runtime→contracts only); seed's only consumer = this call (judge untouched, rule #6). OUTPUT: pass a runtime-local CandidateContent = discriminatedUnion of CandidateIdea.options.omit(KERNEL={id,runId,generationId,agenomeId,status}) → createGateway validate/repair≤1/reject → malformed → graceful agenome.failed (not accept-then-append-crash); omit-set COMPLETENESS is load-bearing (omit too few → valid output spuriously rejected) — pin with a test; rejected/repaired = provider_call_failed no-energy (#8), failure in log = replay-deterministic (#7); ZERO new contract surface · pin: `test/unit/runtime/loop/generationLoop.test.ts` |
| 92 | 2026-06-23 | [MVP status with no event = one reducer reads the authoritative signal](LESSONS.md#92) | winner 'selected' has no candidate.selected event → derive it from run.completed.finalIdeaRef (LESSON §68) in ONE pure current-state reducer (reducers/winner.ts, appended LAST to REDUCERS): one upstream mark lights up lineage-graph (→ web selectWinner), replay-summary, current-state + run-health at once; no projection-file change, projections stay pure transforms (§53/§54); no-op when finalIdeaRef absent / candidate unmaterialized (never fabricates, rule #6); replay-equivalent (mark in the shared fold, rule #7); the CONVERSE of §62 (derive iff there's an authoritative event/payload to key on) · pin: `apps/api/test/unit/projections/winner.test.ts` |
| 93 | 2026-06-23 | [creds-free demo-of-record e2e = commit a fixture once + boot real stack vs real PG + recorded gateway](LESSONS.md#93) | replay calls no provider (rule #7) so the e2e needs no keys: capture a winning fixture ONCE via a gated tool (DOPPL_CAPTURE_FIXTURE=1) driving the REAL worker on a loop-capable recorded fake → commit fixtures/replay/<runId>.json; every CI run validates the artifact + boots bootApp(migrate→seedDemo→start) vs real PG + recorded gateway → asserts terminal + final-idea 'selected' resolves (PD.11 bridge) + zero provider calls + replay-equivalence; extract the loop-capable fake to a shared util (§5); §16 config smokes are honest green-from-start characterization (reuse existing tests, weaken none); re-record on sv bump · pin: `apps/api/test/integration/demo/demo-e2e-smoke.test.ts` |
| 94 | 2026-06-23 | [live e2e asserts deterministic invariants over a non-deterministic run, gated opt-in + keyless mirror](LESSONS.md#94) | the headline demo proof runs LIVE (DOPPL_GATEWAY=live, low cap) asserting INVARIANTS not exact text (terminal · caps #1 · 'selected' winner via PD.11 · energy #8 · no-secret #4); gated describe.skipIf(!OPENROUTER_API_KEY) so keyless /preflight+CI stay green (live ≠ CI gate); rule #7 preserved by capture-from-live + replay (creds-free PD.8a stays the keyless base); de-risk the un-executable live assertions with a KEYLESS MIRROR — same invariant helpers run against the committed recorded run (green in CI, non-vacuous guards §10) so the logic is known-correct before any live run; live capture transient (not committed); live execution is an operator action · pin: `apps/api/test/integration/demo/live-e2e-smoke.test.ts` |
| 95 | 2026-06-23 | [committed .env.example single-sourced from the code allowlist via a drift-guard that imports the constants](LESSONS.md#95) | a drift-guard test IMPORTS REQUIRED_CREDENTIAL_ENV/ENV_ALLOWLIST_VARS/BOOT_ORCHESTRATION_ENV + asserts .env.example keys == their union BOTH directions → can't silently drift from what boot reads; rule #4 pinned (placeholder-only + NON-VACUOUS real-key-shape guard §10) since a committed env template is a secret-leak vector; additive var-NAME exports (never values); strongly single-source the drift-prone allowlists, doc-grade const+comment OK for stable inline-read vars; OMIT vars with no code constant + no wiring (Langfuse P2.8-deferred) rather than invent misleading names · pin: `apps/api/test/unit/config/env-example-drift.test.ts` |
| 96 | 2026-06-23 | [unit-tested helper with no caller is dead — wire at build-time](LESSONS.md#96) | a unit-tested helper that no production entry calls is dead by reachability (green units prove it works, not that anything reaches it) → the /wired / phase-exit gate blocks; wire it into its production entry (route/boot) the SAME slice you build it (or schedule an explicit task), never implicit build-now-wire-later; demo write-path convenience shares + defers to the authoritative boundary (§89) · pin: `apps/api/test/integration/routes/demo-wiring.test.ts` |
| 97 | 2026-06-23 | [shared-container full-stack-boot needs its own fresh DB](LESSONS.md#97) | a boot test (migrate+seed+run) in a shared testcontainer (§25) needs its OWN fresh `CREATE DATABASE` per boot (track names + `DROP …WITH FORCE` after closing pools), not just a unique runId — else migrations/seed/run-enum collide with the committed fixture + sibling tests · pin: `apps/api/test/integration/demo/live-e2e-smoke.test.ts` |
| 98 | 2026-06-23 | [json_object + schema-as-candidate-independent-system-text](LESSONS.md#98) | a discriminated-union schema (root anyOf/optional) → HTTP 400 on OpenAI's strict subset (curl-confirmed BOTH strict modes); relax response_format to json_object + convey the shape as a candidate-INDEPENDENT system instruction (derived from the request schema, never candidate text); the gateway's Zod validate/repair/reject stays authoritative (rule #5 UNWEAKENED, §23), §38 intact; live-validated (PD.8c 10/10, real winner) · pin: `apps/api/test/unit/model-gateway/adapters/openrouter.adapter.test.ts` |
| 99 | 2026-06-23 | [relative .env.example path breaks per-pkg run; silent boot needs a stdout log](LESSONS.md#99) | a committed .env.example RELATIVE path (DOPPL_FIXTURE_DIR=fixtures/replay) breaks `pnpm -C apps/api start` (CWD→ENOENT) → OMIT it + rely on the module-relative default (pinned CWD-independent); §95 drift-guard treats it as an explicit CODE_DEFAULTED_OMITTABLE (REQUIRED set still pinned, not a silent weakening); a silent boot (Fastify logger off → app.log no-op) needs a console.log host:port after listen (outside rule-#4 boundary, not a run_event) · pin: `apps/api/test/integration/boot/main-boot.test.ts` |
| 106 | 2026-06-24 | [a generation diverge/converge dial = a system-authored band fragment + a clamped temperature on the GENERATION request ONLY; the judge/critic path gets neither (rule #6 SOLO, structural)](LESSONS.md#106) | a diverge/converge (or any generation-shaping) dial is a framing band (system-authored, rule-#6-clean, closed-set — like the FB.3 operators §104) PLUS a clamped temperature nudge `clamp(0.7+0.3·bias,[0.4,1.2])` (research-bounded ≤1.2 ceiling) set on the `population_generator` request's `samplingParams` ONLY; the rule-#6 SOLO pin is STRUCTURAL — the single `assembleIsolatedRequest` chokepoint that builds every critic/judge request has no bias/samplingParams field in its param type, so the dial is unable to reach the evaluation path by construction (stronger than a byte-identical-anchor test); engage the dial only when non-neutral so an absent/neutral dial stays byte-identical to the baseline; thread via mergePerRunConfig with `!== undefined` (preserve explicit 0); RECORD the executed samplingParams into llm_call_telemetry so replay reads it, never re-derives via the bias→temp fn or re-samples (rule #7); the ACTUAL behavioral effect is a model-dependent live `/eval` (novelty axis), not a unit assertion; additive contract sv7→8 (samplingParams optional on ModelGatewayRequest + LlmCallTelemetry, strictObject = no secret field — rule #4) · pin: `apps/api/test/unit/runtime/loop/generationBias.test.ts` + the FB.4 generationLoop rule-#6 two-sided test · accepted: convention (generation-dial / rule-#6 SOLO structural isolation) |
| 105 | 2026-06-24 | [secret-surface raw-capture event = truncate-with-marker under the ceiling, then RIDE the existing scrub seams (no 3rd scrub); replay no-provider; not a spend](LESSONS.md#105) | capturing a successful generation LLM call's raw response/reasoning as a queryable event is rule-#4 secret-surface by REUSE: a new high-traffic event (llm_call_telemetry + LlmCallTelemetry, modeled on EnergyEvent, REQUIRED queryable `truncated` flag); a raw field over budget is TRUNCATED-WITH-MARKER by a pure helper (~384 KiB/field, byte/multibyte-safe — the ceiling REJECTS oversized so truncate is mandatory, never reject), then rides the EXISTING scrubEventPayload (append) + scrubObservabilityPayload (Langfuse) — no 3rd scrub, truncate-before-scrub doesn't leak (a straddling secret keeps ≥ the pattern floor after sk-→redacted, or a sub-usable fragment); replay reads it no-provider (rule #7); not a spend (caps/energy unchanged, a failed call appends none — rule #1/#8); GENERATION emit point only (judge rationale=FB.8, fusion=selection seam) · pin: `apps/api/test/unit/event-store/truncate-capture.test.ts` · accepted: convention (secret-surface telemetry capture) |
| 104 | 2026-06-24 | [mutagen operators = TRUSTED closed-enum framing in the SYSTEM message, NOT isolated DATA — the two channels stay separate](LESSONS.md#104) | a generation operator/strategy/lens control is a CLOSED enum → SYSTEM-AUTHORED vetted fragments composed into the TRUSTED system message (steering generation), NOT untrusted DATA (which would neuter it); rule #5 holds because closed-enum→no free-text→no injection vector + the untrusted problem stays wrapUntrusted in the USER msg (the two channels — trusted instruction vs untrusted subject — stay strictly separate); threaded via mergePerRunConfig (recorded==executed); pure assembly → replay-deterministic (rule #7); prompt-only, caps/energy untouched (rule #1/#8); no fragment references judge/rubric/scoring/fitness (rule #6) · pin: `apps/api/test/unit/runtime/loop/generationOperators.test.ts` · accepted: convention (generation-framing/two-channel isolation) |
| 103 | 2026-06-24 | [integration route tests need a UNIQUE id prefix — the shared testcontainer DB collides generic ids across files](LESSONS.md#103) | the integration suite shares ONE testcontainer PG across files (§25) → a route test appending run_events with a generic id-N collides with another file's event ids on the unique pk (500, GREEN in isolation / RED in full suite, keyed on file order); prefix the test's generated ids with a UNIQUE per-file token (e.g. mro-) so appends don't collide; distinct from §97's per-boot fresh CREATE DATABASE (full-stack boot tests) — this is the lighter route-test case · pin: the FB.2 integration test (mro- prefix) · accepted: convention (integration test hygiene) |
| 102 | 2026-06-24 | [per-run modelRouteOverride = clamp-as-hint to a FROZEN allowlist at TWO gates (route-422 + kernel-bound overlay re-clamp); excludes the held-out judge (rule #6)](LESSONS.md#102) | a per-run model-route override is clamp-as-hint to a FROZEN per-role allowlist (rule #1, like caps §80): (1) modelRouteOverrideViolation (mirrors overCapField) → POST /runs 422 BEFORE the run.configured append (rule #2); (2) mergePerRunConfig threads the validated override; (3) honor via a per-run registry OVERLAY (createRegistryOverlay re-clamps to the allowlist — applies permitted, ignores non-permitted→base) injected into a per-run gateway → FB.1 dispatch picks the provider; the DOUBLE gate is the point — rule #1 is KERNEL-bound not route-only, a direct-append bypass still can't widen (mirrors the caps re-clamp); (4) replay builds no factory → no provider (rule #7); allowlist covers GENERATION roles (population_generator+fusion_synthesis), EXCLUDES final_judge (rule #6 — judge model not run-swappable), fail-closed, {provider,modelId}-only (rule #4) · pin: `apps/api/test/{unit/model-gateway/model-route-override,integration}` |
| 101 | 2026-06-24 | [2nd live provider = a provider→providerCall dispatch in createLiveGateway (map = runtime allowlist); keyless local provider loads a base-URL config](LESSONS.md#101) | adding ollama as a 2nd live provider is a RUNTIME change not a contract one (provider is an open string) → createLiveGateway becomes provider-dispatching (route.provider→{openrouter→orCall, ollama→ollamaCall}; unknown→honest 0-token ProviderCallError = the runtime allowlist, no silent fallback); register currently-configured non-{or,ollama} providers (openai/web-search) as a LEGACY PASSTHROUGH so embedding/retrieval ride their exact pre-change path (Option A, min blast radius; embedding-live gap = documented TODO); new adapter mirrors lesson 28 (HTTP behind ONE client factory rule #9, raw output, withRetry, 0-token ProviderCallError rule #8); keyless local provider loads OLLAMA_BASE_URL (no key, don't touch REQUIRED_CREDENTIAL_ENV — lazy/keyless cf. lesson 29); structured via format:json + candidate-independent instruction (rule #5, gateway stays authoritative cf. lesson 98); replay builds no client (rule #7) · pin: `apps/api/test/unit/model-gateway/{adapters/ollama.adapter,live-gateway-dispatch}.test.ts` · accepted: convention (gateway/provider) |
| 100 | 2026-06-24 | [additive RunConfig amendment touches a fixed doc/test set; the schemaVersion pin lives in 4 test spots](LESSONS.md#100) | a run-control add to a frozen shared model (RunConfig) = OPTIONAL + additive + ONE monotonic CURRENT_SCHEMA_VERSION bump; same-cycle surface = schema file + barrel export + version constant + FIELD_SET_SNAPSHOTS + a canonical-fixture variant + the per-topic field-set test; the version pin lives in 4 spots NOT 2 (`field-sets.test.ts` + `envelope.test.ts current_schema_version_is_N` + `fixtures-valid.test.ts canonical_fixtures_still_valid_at_current_version` + the `src/version.ts` constant) → grep `CURRENT_SCHEMA_VERSION`/`toBe(<old>)` across `packages/contracts/test` before a bump or a stale `toBe` slips through green-locally; a new closed enum also joins the `contract-surface.test.ts` UNION_SWEEP (keeps the out-of-set rejection gate exhaustive); assert the immutable rule-#6 surface (ScoringPolicy/FinalJudgeRubric/FinalJudgeAxis) byte-identical in the SAME slice (a guard that rides the amendment to prove non-effect) · pin: `packages/contracts/test/__schema-snapshots__/fb0-run-controls.test.ts` · accepted: convention (contract-amendment process) |

<!-- Starts empty. Each row links to its `LESSONS.md` anchor. -->
